# 个人工作助手 · 需求方案设计文档（PRD）

> 版本：v1.2 · 状态：已通过 grilling 盘问 + 自评审 + 架构师筛选，待开发执行
> 最后更新：2026-08-03
> 性质：**需求设计文档，不含代码实现**

---

## 0. 文档目的与变更记录

本文档把"用 AI agent 做一个个人工作日常助手"这个粗略想法，通过盘问（grilling）层层拆解后，固化为可执行的需求方案。**回答：是什么/怎么用/怎么做/怎么验收。不回答具体代码。**

### 文档结构（按版本递进阅读）

| 部分 | 章节 | 内容 |
|---|---|---|
| **v1 基础** | §1–10 | 定位、范围、场景、功能、数据模型、技术栈、验收、风险、里程碑 |
| **v1.2 增量** | §11–15 | UI 重设计 + 4 基础工具 + 9 参考增补工具 |
| **V2 展望** | §16 | 未来路线图（明确推迟的方向） |
| **附录** | A | 统一决策溯源 + 设计 skill 诚实声明 |

### 变更历史

**v1.0 → v1.1（自评审）：**
- 🔴 补回遗漏的**联网搜索**功能模块与验收项
- 🔴 补全 **Conversation / Message / Provider / Settings** 数据模型
- 🟠 加固 `write_file`（覆盖前确认 + 回收站）
- 🟠 任务抽取从"每轮"改为**可配置 + 防抖 + 用最便宜模型**
- 🟠 性能指标拆分为软件侧/模型侧
- 🟠 新增智谱 FC 兼容性的技术验证项
- 🟡 补全上下文截断、跟进模型指定、流式 IPC、错误重试、离线可用性
- 🟡 新增"靠 AI 写代码"的项目方法论约束

**v1.1 → v1.2（UI 重设计 + 工具扩展）：**
- 🎨 §12 **UI 重设计**：从 shadcn 默认重定向为**冷调科技风**（Linear/Vercel 系），双主题，统一 lucide 图标，无圆角色条消息签名
- 🔧 §13 **4 基础工具**：快速笔记、提醒+番茄钟、文档转换（双轨制架构：FC + 手动页面）
- 📚 §15 **9 参考增补工具**：参考多宝鱼 + WorkBuddy 后筛选——笔记增强（AI助手/待办转任务/大纲）、AI 产出（日报/思维导图）、文档增强（PDF/OCR/截图/表格）
- 🗂 §16 V2 路线图重组：v1.2 已落地的不再列入，补充笔记双链/思维导图视图/PDF压缩等明确推迟项
- 📋 附录统一合并为 §A，消除此前三个分散的附录块
- 🏗 **文档结构修正**：v1.2 章节（原错置于 V2 之后）移至 V2 之前，恢复 v1 → v1.2 → V2 的正确版本递进

---

## 1. 一句话定位

> **一个跑在你自己电脑上的桌面应用。它是一个能接入多家国产大模型 API 的智能对话框，能在对话中帮你抽出并管理任务，能联网搜索、读写你指定位置的文件，还能在固定时间点主动叫醒、跟进你的待办、用桌面通知提醒你。**

所有数据存在你本机。朋友想用？把安装包发给他，他自己装、自己填 Key、数据互不相通。

---

## 2. 范围界定（Scope）—— 最重要的一节

### 2.1 v1 做什么（In Scope）

| 模块 | 做什么 | 不做什么 |
|---|---|---|
| **多模型对话** | 接入智谱 GLM、DeepSeek 等国产模型；设置里填 Key；一个对话里能切换模型；可按"场景"指定默认模型（P1） | 不做模型自动路由（按任务难度自动选便宜/贵的模型，留到 v2） |
| **任务管理** | 手动增删改查待办；AI 从对话中**自动抽取**任务（说"明天下午 3 点开会"→ 自动建任务） | 不做甘特图、不做项目层级、不做多人协作分配 |
| **AI 主动跟进** | 常驻系统托盘 + **固定时间点**（如每天 9:00、14:00）AI 扫描任务列表 + 桌面通知 + 能根据你的回复改任务状态 | 不做高频轮询；不做"帮你起草报告/收集资料"等产出型 agent 动作 |
| **联网搜索** | AI 通过 Function Calling 调用搜索 API（如 Tavily/Bing），把结果整合进回答 | 不做浏览器自动化、不做网页长截图、不做实时信息订阅 |
| **文件读写** | 两种模式：① 手动拖单个文件进对话，AI 读它；② 预设 1~3 个"工作目录"，AI 能在其中读写（**写前必须确认 + 覆盖进回收站**） | 不做全盘自由搜索；不做 RAG 向量检索 |

### 2.2 v1 不做什么（Out of Scope）—— 明确砍掉

- ❌ **不做云同步 / 服务器 / 账号登录。** 纯本地，各自安装各自用。
- ❌ **不做多用户协作、任务共享。** "小圈子可用"= 安装包能分享，不是数据共享。
- ❌ **不做知识库 / RAG / 文档向量化检索。**
- ❌ **不做模型自动路由。** v1 用"手动切 + 按场景指定"。
- ❌ **不做 AI 起草产出物**（写报告、自动收集资料）。这是 C 档 agent。
- ❌ **不做调用系统命令 / 执行 shell。** 安全风险高。
- ❌ **不做移动端 / Web 端。** 仅桌面。
- ❌ **不做语音输入输出。**

### 2.3 重要的预期澄清

> **"个人 + 小圈子可用" 与 "纯本地各自装" 不矛盾，但你要明白：v1 的"小圈子"只是"安装包能分享"，不存在任何形式的协作或数据互通。** 想要"和朋友共用任务列表"是 v2 的服务器版本，是另一个量级。先校准预期，避免做完失望。

---

## 3. 目标用户与使用场景

### 3.1 用户画像
- **主用户**：你自己（混合型工作者，任务类型多样，非单一职业）
- **次要用户**：你分享安装包给的朋友/同事（各自独立）

### 3.2 核心使用场景（User Story）

| # | 场景 | 触发 | 期望结果 |
|---|---|---|---|
| US-1 | 日常问答 | 在聊天框输入问题 | 选定模型流式回答，可追问、可中途切模型 |
| US-2 | 对话生任务 | 聊天里说"周五前交季度复盘" | AI 抽出任务草稿，确认后入库 |
| US-3 | 管理待办 | 打开任务视图 | 查看/勾选完成/改截止日/删 |
| US-4 | 定时跟进 | 每天定点（9:00） | AI 扫任务，挑"今天到期/逾期/重要"的，弹通知问进展 |
| US-5 | 跟进对话 | 点通知回复 | 告诉 AI 进展，AI 据此更新任务备注/状态 |
| US-6 | 联网搜索 | 问"最新的 XX 政策" | AI 自动调搜索工具，整合实时结果回答 |
| US-7 | 读文件答疑 | 拖一份 PDF/Markdown 进对话 | AI 读内容，回答关于这份文件的问题 |
| US-8 | 工作目录 | 预设"我的笔记"目录 | AI 能在该目录读写（写前确认，覆盖进回收站） |
| US-9 | 后台常驻 | 关掉主窗口 | 缩到托盘继续运行，定时跟进照常 |

---

## 4. 功能需求详述

### 4.1 模块一：多模型对话

**F1.1 模型接入（Provider 抽象层）**
- 接入目标：**智谱 GLM、DeepSeek** 为 v1 必做；架构上预留"任意 OpenAI 兼容接口"接入能力。
- 关键事实（来自官方文档确认）：
  - **DeepSeek**：完全兼容 OpenAI SDK，`baseURL='https://api.deepseek.com'`，模型 `deepseek-chat`，**确认支持 `tools`（Function Calling）**。
  - **智谱**：提供 OpenAI 兼容端点可统一走 OpenAI SDK；**但其兼容端点是否完整支持 `tools` 需在 M1 阶段实测验证**（见 §10 技术验证项 TV-1）。若不支持，任务抽取等 FC 依赖功能需回退到"结构化 JSON 输出解析"方案。
- 抽象设计：统一 `Provider` 接口，每家模型实现一份适配器，对外暴露统一 `chat(messages, tools)` 方法。

**F1.2 API Key 管理**
- 设置页填写各家 Key，**用 Electron `safeStorage` 加密存储**（不明文落库）。
- 支持"测试连接"（发 ping 请求验证 Key 有效）。

**F1.3 会话管理 + 上下文截断（明确策略）**
- 多会话（左侧列表，可新建/重命名/删除/置顶）。
- 单会话：流式输出、可中途切模型（后续轮次用新模型）。
- **上下文截断策略（v1 固定规则）：**
  - 按 **token 预算**管理（非条数），每模型设上限（如 32k）。
  - 截断顺序：永远保留 `系统提示` + `最近 N 轮`；从最旧的用户消息开始丢弃。
  - 触发截断时，UI 给出"已省略较早的 X 条消息"提示，不静默丢。
  - 任务抽取不依赖被截断的历史（只看当前轮 + 紧邻上文）。

**F1.4 场景化默认模型（P1，非阻塞）**
- 可定义"场景"（翻译/写报告/闲聊），每个场景绑定默认模型 + 系统提示词。
- 新建会话时可选场景，自动套用。**优先级：P1，v1 可不做。**

### 4.2 模块二：任务管理

**F2.1 数据模型（核心实体定义）**

> v1.1 补全：除 Task 外，新增 Conversation / Message / Provider / Settings 模型。

**Task（任务）—— 中性通用，不预设职业：**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| title | string | 标题 |
| description | string? | 详情 |
| status | enum | todo / in_progress / done |
| priority | enum | low / medium / high |
| dueDate | datetime? | 截止时间 |
| remindTimes | datetime[] | 跟进时间点（可多个） |
| source | enum | manual / from_chat |
| sourceConversationId | string? | 来自哪个对话（可溯源） |
| followupLog | text | AI 跟进进展（追加式） |
| createdAt / updatedAt | datetime | 时间戳 |

**Conversation（会话）：**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| title | string | 自动用首条消息生成，可改 |
| type | enum | normal / followup（跟进会话） |
| scenarioId | string? | 关联场景（P1） |
| defaultProviderId | string? | 本会话默认模型 |
| pinned | boolean | 置顶 |
| createdAt / updatedAt | datetime | |

**Message（消息）：**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| conversationId | string | 外键 |
| role | enum | system / user / assistant / tool |
| providerId | string? | 本条用的模型（支持会话内切换） |
| content | text | 文本内容 |
| toolCalls | json? | Function Calling 调用记录 |
| attachments | json? | 拖入的文件引用 |
| createdAt | datetime | |

**Provider（模型配置）：**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| name | string | 显示名（如"DeepSeek-V3"） |
| type | enum | deepseek / zhipu / custom |
| baseURL | string | 接口地址 |
| model | string | 模型标识 |
| apiKeyRef | string | 指向 safeStorage 的引用（不存明文） |
| enabled | boolean | 是否启用 |

**Settings（设置，KV 表）：** 存储定时时间点、人格选择、工作目录白名单、截断预算、任务抽取开关等。

**F2.2 AI 任务抽取（修订：可配置 + 防抖，不再每轮强跑）**
- **触发方式（修订）：** 默认**手动**——用户点"✨ 抽取任务"按钮触发；可在设置里开"自动抽取"，开启后**防抖**（停止输入 5 秒、或一轮对话结束后才跑一次，不逐字跑）。
- 用**最便宜的模型**（如 deepseek-chat 而非推理模型）跑抽取，省成本。
- 抽取结果 = 任务草稿，**必须人工点"加入任务"才入库**（不静默建）。
- 抽取失败/超时 → 静默忽略，不打扰对话。
- 草稿入库时自动填 `source=from_chat` + `sourceConversationId`。

**F2.3 手动任务 CRUD**
- 列表/看板两种展示（看板为 P1）。
- 字段编辑：标题、详情、状态、优先级、截止日、跟进时间。

### 4.3 模块三：AI 主动跟进

**F3.1 常驻与托盘**
- 关闭主窗口 → 最小化到系统托盘（不退出）。
- 托盘菜单：显示主窗口 / 立即检查 / 暂停跟进 / 退出。
- 单实例锁。

**F3.2 定时调度**
- 调度器跑在**主进程**（不依赖前端页面）。
- 默认时间点：09:00、14:00（可改）。
- **到点流程：**
  1. 查"今天到期 / 已逾期 / 高优先级未完成"任务。
  2. **无候选任务 → 不调用模型，不发通知**（省 API）。
  3. 有候选 → 用**用户设定的"跟进模型"**（默认与默认 Provider 同；可在设置单独指定一个便宜模型）+ 所选人格 prompt 生成跟进消息。
  4. 弹系统桌面通知，点击进入跟进会话。
- **未配置任何模型 Key 时：** 不调用，托盘显示"⚠ 未配置模型，跟进已暂停"，不报错。

**F3.3 跟进对话与任务联动**
- 跟进 = type=followup 的特殊会话。
- AI 问进展 → 你回复 → AI 通过 FC：
  - 标记完成 / 改截止日 / 拆子任务（**B 档允许的状态修改**）。
  - 把回复追加到该任务 `followupLog`。
  - **状态修改类操作二次确认**（FC 返回意图 → UI 弹"确认完成「XX」？"→ 确认才执行），防误改。
- **不做**：起草报告、自动收集资料。

**F3.4 人格语气（可选，P1）**
- 设置可选：克制型秘书 / 损友型 / 纯工具型。影响 system prompt。

### 4.4 模块四：联网搜索

**F4.1 搜索工具**
- 通过 FC 暴露 `web_search(query)` 工具给模型。
- v1 接一个搜索 API：**Tavily**（AI 友好、有免费额度）或 **Bing Search**，二选一（开发阶段定，见 §10）。
- Key 在设置页填，同模型 Key 一样加密存储。
- 结果整合：模型把搜索结果摘编进回答，并在回答里标注来源链接。

### 4.5 模块五：文件读写（修订：加固写入安全）

**F5.1 手动拖入**
- 对话框拖拽单个文件（.txt / .md / .pdf / .docx 等）。
- AI 读内容作上下文。大文件截断（前 N 字 + "已截断"提示）。

**F5.2 预设工作目录（写入安全加固）**
- 设置里配置 1~3 个白名单目录。
- FC 工具 `read_file(path)` / `write_file(path, content)`，**主进程硬校验白名单**，越界拒绝 + 日志。
- **🆕 写入三重防护（v1.1 加固）：**
  1. **路径校验：** 解析真实路径（防 `../` 逃逸），必须在白名单内。
  2. **覆盖确认：** 目标文件已存在 → UI 弹"AI 要覆盖 `报告.docx`，确认？"，拒绝则不写。
  3. **回收站：** 覆盖前把原文件移到 `userData/fileTrash/{timestamp}/`，保留可恢复；定期清理（默认 7 天）。
- 文件大小上限（如单次写 5MB），防异常。

### 4.6 模块零：基础设施

| 项 | 方案 |
|---|---|
| 数据存储 | SQLite 单文件库（`app.getPath('userData')/app.db`） |
| 设置/Key 加密 | Electron `safeStorage` |
| 错误处理 | 统一重试策略（见 §5） |
| 日志 | 本地日志文件 |
| 离线降级 | 见 §5 |
| 自动更新 | 预留 electron-updater（v1 可不强求） |
| 打包 | electron-builder，Windows 安装包 |

---

## 5. 非功能需求

| 维度 | 要求 |
|---|---|
| **安全** | API Key `safeStorage` 加密；文件白名单 + 写入三重防护；不执行系统命令；网络仅指向配置的模型/搜索 API |
| **隐私** | 全本地；仅你主动调模型/搜索 API 时发送对话内容 |
| **性能** | **软件侧：** 冷启动 < 3s；UI 操作响应 < 200ms；流式 IPC 转发延迟 < 100ms。**模型侧：** 首字延迟取决于厂商（不硬性承诺，但 UI 显示 loading + 可取消） |
| **可靠性** | 调度在主进程不因关窗失效；模型 API 错误**指数退避重试 3 次**（429/超时/5xx），仍失败则友好提示；跟进失败通知"请检查网络/Key" |
| **离线可用** | 断网时：任务 CRUD、查看历史对话、托盘常驻**仍可用**；仅模型对话/搜索/跟进调用**降级**（灰掉按钮 + 提示） |
| **可维护（本项目特有）** | TS 严格类型；Provider 抽象；**强制维护 `AGENTS.md` + `CONTEXT.md` 让 AI 持续理解项目**（见 §6.4） |

---

## 6. 技术方案（概要）

### 6.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 桌面框架 | **Electron** | AI 语料最丰富，靠 AI 生成代码出错率最低 |
| 前端 | **React + TypeScript** | 主流、AI 友好 |
| UI | **shadcn/ui + Tailwind** | 现代美观，AI 易生成 |
| 状态 | **Zustand** | 轻量、样板少 |
| 数据库 | **SQLite**（Prisma） | 单文件、类型安全 |
| 模型调用 | **OpenAI SDK**（统一封装） | DeepSeek 完全兼容；智谱走兼容端点，一套代码接多家 |
| 打包 | **electron-builder** | 主流，支持 Win 安装包 + 自动更新 |

### 6.2 关键技术决策（文档确认）

- **模型接入**：OpenAI SDK 统一封装，`baseURL` + `apiKey` 切换 Provider。FC（`tools`）用于任务抽取、状态修改、文件读写、搜索。
- **进程模型**：主进程跑托盘/单实例/调度/文件 IO/模型调用/搜索；预加载脚本用 `contextBridge` 暴露**受限** IPC；渲染进程跑 React。
- **流式 IPC**：主进程 SSE/流式接收模型输出 → 通过 `webContents.send` 增量推送 → 渲染进程逐字渲染（不阻塞 UI）。
- **安全**：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。

### 6.3 项目结构（建议）

```
个人工作助手/
├── electron/                  # 主进程
│   ├── main.ts                # 入口：窗口/托盘/单实例
│   ├── scheduler.ts           # 定时跟进调度器
│   ├── ipc/                   # IPC handlers
│   └── services/
│       ├── providers/         # 各家模型适配器
│       ├── fileTools.ts       # 白名单 + 三重防护
│       ├── searchTools.ts     # 联网搜索
│       └── taskExtractor.ts   # 任务抽取
├── src/                       # 渲染进程 (React)
│   ├── pages/                 # 对话页/任务页/设置页
│   ├── components/
│   └── stores/                # Zustand
├── prisma/
│   └── schema.prisma          # 数据模型
├── AGENTS.md                  # 🆕 给 AI 的项目指南
├── CONTEXT.md                 # 🆕 领域语言/架构决策
└── docs/
    └── 需求方案设计文档.md      # 本文档
```

### 6.4 🆕 "靠 AI 写代码"的方法论约束（本项目特有）

> 你不打算手动改代码、全靠 AI 生成。这有一个**致命前提**：AI 必须能持续、一致地理解这个项目，否则改 A 坏 B，几轮后项目就烂掉。强制做三件事：

1. **`AGENTS.md`**（项目根）：写明技术栈、目录结构、编码规范、禁忌（如"不要在渲染进程直接调 Node API"）。每次让 AI 改代码前，它先读这个。
2. **`CONTEXT.md`**（领域语言）：固化核心术语（Provider、跟进会话、工作目录、任务抽取草稿等），避免 AI 每次重新发明命名。
3. **小步生成 + 逐步验证**：每个功能拆成能独立跑通的最小块，生成一块、验证一块、提交一块，不允许"一次性生成大块"。

---

## 7. v1 验收标准（"做完"的定义）

> 对抗范围膨胀的锦囊。**全部 P0 满足 = v1 完成。**

### 7.1 P0（必须）

- [ ] **V-A 能聊天**：填智谱或 DeepSeek Key，能流式对话。
- [ ] **V-B 能切模型**：会话内切换模型，切换后正常。
- [ ] **V-C 能测 Key**：设置页"测试连接"验证 Key。
- [ ] **V-D 任务手动管**：新建/编辑/完成/删除。
- [ ] **V-E 对话抽任务**：抽取草稿 → 确认入库（默认手动触发，自动模式为可选）。
- [ ] **V-F 常驻托盘**：关窗缩托盘，托盘菜单，单实例。
- [ ] **V-G 定时跟进**：到点扫任务 + 弹通知（**提供"调试触发"按钮**立即测试，不必等真实到点）。
- [ ] **V-H 跟进改状态**：跟进对话回复后，AI 改任务状态（二次确认）。
- [ ] **V-I 联网搜索**：问实时信息，AI 调搜索工具整合结果 + 标来源。
- [ ] **V-J 拖文件**：拖文件进对话，AI 读内容回答。
- [ ] **V-K 工作目录**：配置目录，AI 能读写；**写覆盖弹确认 + 进回收站**；越界拒绝。
- [ ] **V-L 数据本地**：全部数据 SQLite 本地，Key `safeStorage` 加密。
- [ ] **V-M 离线降级**：断网时任务 CRUD/历史对话可用，模型功能灰掉提示。

### 7.2 P1（可选，做完更好）

- [ ] 人格语气切换 / 场景化默认模型 / 任务看板 / PDF 解析 / 自动任务抽取模式

### 7.3 明确不算 v1
云同步、账号、协作、RAG 知识库、模型自动路由、AI 起草产出、移动端、语音。（见 §2.2、§11）

---

## 8. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| **范围再膨胀** | 高 | 严守 §7；新需求默认进 v2 |
| **定时跟进烧 API** | 中 | 固定时间点；无候选不调用；跟进用便宜模型 |
| **🆕 任务抽取烧 API/卡顿** | 中 | 默认手动触发；自动模式防抖；用最便宜模型 |
| **🆕 智谱不完整支持 FC** | 中 | M1 实测（TV-1）；不支持则回退结构化 JSON 解析 |
| **靠 AI 生成代码出错难调** | 高 | 最主流栈；AGENTS.md/CONTEXT.md；小步验证（§6.4） |
| **国产模型 FC 不稳** | 中 | 抽取人工确认；状态修改二次确认 |
| **🆕 write_file 覆盖重要文件** | 中 | 写入三重防护（路径校验+覆盖确认+回收站） |
| **托盘/调度 Win 行为异常** | 中 | 测试关窗、休眠唤醒后调度 |
| **🆕 模型 API 限流/超时** | 中 | 指数退避重试 3 次，失败友好提示 |
| **期望"小圈子=协作"** | 中 | §2.3 已澄清，README 重申 |

---

## 9. 里程碑建议（不赶时间，仅参考）

| 阶段 | 目标 | 对应验收 | 必做技术验证 |
|---|---|---|---|
| M1 骨架 | Electron+React 跑起来，调通一家模型，**实测 FC** | V-A | **TV-1 智谱 FC** |
| M2 对话完整 | 会话管理、切模型、Key 管理、截断 | V-B V-C | |
| M3 任务基础 | 手动 CRUD + 完整数据模型 | V-D | |
| M4 对话→任务 | AI 抽取任务 | V-E | |
| M5 搜索+文件 | 联网搜索 + 拖文件 + 工作目录 | V-I V-J V-K | |
| M6 常驻+跟进 | 托盘、调度、通知、跟进对话 | V-F V-G V-H | |
| M7 打磨打包 | 错误重试、离线降级、打包 | V-L V-M + 全部 | |

---

## 10. 技术验证项与开放问题

**技术验证（M1 阶段必做）：**
- **TV-1**：智谱 OpenAI 兼容端点是否完整支持 `tools`（Function Calling）。决定 FC 依赖功能能否成立。
- **TV-2**：智谱 vs DeepSeek 谁更稳更便宜用于"任务抽取"和"跟进"这两个后台调用。
- **TV-3**：搜索 API 选 Tavily 还是 Bing（免费额度、结果质量、是否被墙）。

**开放问题（开发阶段定）：**
1. 智谱走兼容端点 vs 官方 SDK（建议先兼容端点统一封装）。
2. 大文件解析库选型（pdf-parse / mammoth）。
3. 跟进通知在 Windows 焦点辅助下的表现，真机验证。
4. 是否提供"开机自启"开关（建议 P1）。
5. 截断 token 预算默认值（建议 32k，按模型上限动态调）。

---

## 11. v1.2：UI 重设计 + 工具扩展

> 版本：v1.2（基于 v1.1 实际实现做的增量设计）
> 状态：待评审
> 日期：2026-08-03
> 触发：用户反馈"UI 太丑、图标与内容反差大"+"工具太少"，并参考两个工作台视频

v1.2 三大改动：
- 🎨 **UI 重设计**（§12）：从"无身份的 shadcn 默认"重定向为**冷调科技风**（Linear/Vercel 系），双主题，统一 lucide 图标系统。
- 🔧 **4 基础工具**（§13）：快速笔记、提醒+番茄钟、文档转换。双轨制架构（FC + 手动页面）。
- 📚 **9 参考增补工具**（§15）：参考多宝鱼 + WorkBuddy 后筛选——笔记增强 / AI 产出 / 文档增强。

> ⚠️ **关于设计 skill 的诚实声明**：用户指定用 `ui-ux-pro-max` skill，但该 skill 在本环境是 catalog-only 空壳（仅元数据，无模板库）。v1.2 UI 设计实际基于 **frontend-design skill** 的设计原则 + **Open Design 默认 design-system 指引** + **对项目实际代码的诊断**。完整模式库需从 https://github.com/nextlevelbuilder/ui-ux-pro-max-skill 安装上游 bundle。详见附录 A.5。

---

## 12. UI 重设计方案

### 12.1 设计定位（一句话论点）

> **"一个长在工作日的、安静而精确的工具。它不喊叫，不卖弄，但它每个像素都在干活。"**

参照系：**Linear（精确）+ Vercel（克制）+ Raycast（密度）**。
反参照：所有用蓝紫渐变 + 圆角气泡 + emoji 图标的 AI 应用（那是默认，不是设计）。

**冷调科技风的内核：极少颜色、hairline 分割、字体精致、留白克制、一个签名元素撑起辨识度。**

### 12.2 视觉系统（Token 完整定义）

#### 12.2.1 色板（双主题，HSL 写进 CSS 变量）

> 实现位置：`src/index.css`，替换现有 `:root` 与新增 `.dark`。所有业务组件**禁止**再用 `bg-slate-100` 等原生调色板，必须走 token。

```css
/* 浅色（默认）*/
:root {
  --background: 0 0% 98%;           /* #FAFAFA 冷白 */
  --surface:    0 0% 100%;          /* #FFFFFF 卡片表面 */
  --foreground: 240 10% 9%;         /* #18181B 正文近黑 */
  --muted-foreground: 240 4% 46%;   /* #71717A 次文 */
  --primary:    240 10% 4%;         /* #0A0A0A 主色（近黑，按钮用）*/
  --accent:     221 100% 50%;       /* #0066FF 强调（链接/焦点/流式指示）*/
  --border:     240 6% 90%;         /* #E4E4E7 hairline */
  --ring:       221 100% 50%;       /* 焦点环 */
  /* 语义色（取代散落的 red/green/amber/purple）*/
  --success: 142 71% 45%;           /* #22C55E */
  --warning: 38 92% 50%;            /* #F59E0B */
  --danger:  0 84% 60%;             /* #EF4444 */
  --info:    221 100% 50%;          /* 同 accent */
}

/* 深色 */
.dark {
  --background: 0 0% 4%;            /* #0A0A0A */
  --surface:    240 6% 10%;         /* #16161A */
  --foreground: 0 0% 98%;           /* #FAFAFA */
  --muted-foreground: 240 5% 65%;   /* #A1A1AA */
  --primary:    0 0% 98%;           /* 反相：深色下按钮变白底黑字 */
  --accent:     217 91% 60%;        /* #3B82F6（深色下亮一档）*/
  --border:     240 6% 16%;         /* #27272A */
  --success: 142 71% 45%;
  --warning: 38 92% 50%;
  --danger:  0 72% 51%;             /* #DC2626 */
}
```

**强调色选择说明**：用 `#0066FF`（royal blue / 电光蓝）而非默认 sky blue（#0EA5E9，太浅太"互联网"）或 violet（AI 默认）。它更深、更冷、更"工程感"，是 Stripe/Linear 的共同选择。这是冷调科技风的合理归宿——冷调本质就是蓝青系。若你强烈反蓝，备选 `#06B6D4`（cyan，更冷更稀有）。

#### 12.2.2 字体（不引入额外字体文件，纯系统栈 + 本地化 Inter）

```css
--font-sans: "Inter", -apple-system, "PingFang SC", "Microsoft YaHei",
             "Source Han Sans CN", system-ui, sans-serif;
--font-mono: "JetBrains Mono", "Geist Mono", "Cascadia Code",
             Consolas, monospace;
```

- Inter 通过 `@fontsource/inter` 本地化打包（不依赖 Google Fonts，离线可用）。
- **不用衬线体**——冷调科技风是 sans 的主场。中英文都用无衬线，统一现代感。
- 字号阶梯（固定 6 档，禁止随意 px）：`xs=12 / sm=13 / base=14 / lg=16 / xl=20 / 2xl=24`。

#### 12.2.3 图标系统统一规范（修"图标反差"的核心）

> **诊断**：当前 `lucide-react@0.460` 已装但零使用，图标退化成手写 SVG + emoji + 文字三套混搭，视觉重量极不协调。

**统一规则：**
- **唯一图标库**：`lucide-react`，禁用任何 emoji 作为功能图标、禁用文字字符（`+`/`×`/`我`/`AI`）。
- **全局参数**：`size={18}`、`strokeWidth={2}`（与字体重量协调）。
- **封装 `<Icon>` 组件**：统一 import 路径、统一 size/stroke，避免散落。
- **头像**：用 lucide `User` / `Bot` / `Sparkles`（AI）替代汉字"我"/"AI"。

**精确替换清单**（基于代码审计）：

| 文件:行 | 当前 | 替换为 |
|---|---|---|
| `App.tsx:29-44` | 手写 SVG（对话/任务/设置） | `MessageSquare` / `CheckSquare` / `Settings` |
| `ChatPage.tsx:350` | `✨ 抽取任务` | `<Sparkles size={14}> 抽取任务` |
| `ChatPage.tsx:365` | 大 `💬` 空状态 | `<MessageSquare size={48} className="text-muted-foreground/40">` |
| `ChatPage.tsx:462-463` | "我"/"AI" 文字头像 | `<User size={16}>` / `<Bot size={16}>` |
| `ConversationList.tsx:83` | `+ 新会话` | `<Plus size={14}> 新会话` |
| `ConversationList.tsx:142` | `×` 删除 | `<X size={14}>` |
| `TasksPage.tsx:239-241` | 手写勾 SVG | `<Check size={14}>` |
| `TasksPage.tsx:286` | `+ 添加任务` | `<Plus size={14}>` |
| `ConversationList`(流式点) | `bg-blue-500` 小圆点 | `<Loader2 size={12} className="animate-spin">` |

#### 12.2.4 Signature 签名元素（这个应用被记住的点）

frontend-design 原则："花哨只在一处。" 我的签名选择：

> **无圆角消息块 + 左侧 2px role 色条**
>
> 抛弃默认的圆角气泡（所有 AI 应用都那样）。每条消息是一个**左对齐的矩形块**，左侧贴一条 2px 的色条编码角色：
> - **用户消息**：accent 蓝色条（`#0066FF`）
> - **AI 消息**：中性灰色条（`muted-foreground`）
> - **工具消息**：success 绿色条（仅 FC 调用时显示）
>
> 视觉上像一份**精装卷宗的条目**，而非聊天软件。这就是辨识度。

ASCII 预览：
```
┃▌把这份报告总结成三点                    ← 用户(蓝条)
┃                                       
┃▌好的，这份报告的核心是：                ← AI(灰条)
┃  1. ...                                
┃  2. ...                                
```

辅助签名（次要）：所有区块分隔用 **1px hairline**（`border-border`）而非阴影/圆角，营造"工程图纸"精确感。

### 12.3 布局重构（容纳新工具的导航重组）

当前导航只有 3 项（对话/任务/设置）。新增笔记 + 工具后，重组为 **5 项分组导航**：

```
┌────────────────────────────────────────────────────────┐
│ 64px  │  主区                                            │
│ 导航  │                                                 │
│       │                                                 │
│ 💬对话│  [各页面内容]                                    │
│ ✅任务│                                                 │
│ 📝笔记│                                                 │
│ 🔧工具│                                                 │
│ ───── │                                                 │
│ ⚙设置 │                                                 │
│ ☀/🌙 │  ← 主题切换（底部）                              │
└───────┴─────────────────────────────────────────────────┘
```

- 导航图标全部 lucide：`MessageSquare` / `CheckSquare` / `StickyNote` / `Wrench` / `Settings`，底部 `Sun`/`Moon` 切换。
- 64px 宽度不变，新增两项用 hairline 分组（主功能 / 设置 / 主题）。
- **番茄钟**不占导航位，作为**对话页/任意页顶栏的常驻小部件**（一个 25:00 计数 + 开始按钮），点击展开浮层。

### 12.4 三大主视图重设计要点

| 视图 | 当前问题 | v1.2 改动 |
|---|---|---|
| **对话页** | 圆角气泡 + emoji 头像 + `bg-blue-500` 硬编码 | 改无圆角色条消息块；头像换 lucide；流式指示换 `Loader2` 旋转；草稿卡 token 化（amber→`--warning`） |
| **任务页** | 状态徽标用 5 种 Tailwind 原生色 | 改语义 token（`--success/--warning/--danger`）；状态用色条/点而非彩色背景；看板视图 P1 保留 |
| **设置页** | 平铺分区 | 加"外观"分区（主题切换）；语义色预览；保持现有 4 分区 |
| **笔记页**（新）| — | 左列笔记树 + 标签；中间编辑器（Markdown 所见即所得）；右列大纲/反链（P1） |
| **工具页**（新）| — | 卡片网格入口：番茄钟 / 文档转换 / 提醒列表 |

### 12.5 主题切换实现要点

- 新增 `src/stores/theme.ts`（Zustand）：`mode: 'system' | 'light' | 'dark'`。
- `index.html` 根元素加 `.dark` 类（由 preload 注入，避免闪烁）。
- 设置页"外观"分区：三选一（跟随系统/浅色/深色）。
- **所有颜色必须走 token**——这是双主题成立的前提，也是当前代码最大债务（见 12.2.1 末尾禁令）。

### 12.6 UI 重设计验收（v1.2 增项）

- [ ] **V-N 全图标统一**：全项目 `grep` 无 emoji 功能图标、无文字字符图标、无 inline SVG（导航/操作类），全部 lucide。
- [ ] **V-O 配色 token 化**：全项目 `grep` 无 `bg-slate-/bg-blue-/bg-green-/bg-amber-/bg-red-/bg-purple-` 原生类（语义色走 token）。
- [ ] **V-P 双主题**：浅/深切换正常，无白底黑字反转bug，跟随系统生效。
- [ ] **V-Q 签名元素**：消息块为无圆角 + 左侧 role 色条。
- [ ] **V-R hairline 分隔**：区块间用 1px border 而非阴影。
- [ ] **V-S 导航 5 项**：对话/任务/笔记/工具/设置 + 主题切换。

---

## 13. 工具扩展方案

### 13.1 工具架构（我推荐：双轨制）

> 你授权"我推荐"。我的判断：单一架构（全 FC 或全页面）都不对。

**双轨制定义：**

| 轨道 | 形式 | 谁触发 | 例子 | 复用 |
|---|---|---|---|---|
| **A. AI 工具（FC）** | 注册进现有 `assembleTools()` | AI 在对话中调用 | `create_note` `search_notes` `convert_document` `set_reminder` | 复用现有 FC 循环、二次确认机制 |
| **B. 手动页面** | 独立 React 页面 + IPC | 用户主动用 | 笔记编辑器、番茄钟、转换器 UI | 复用 IPC 白名单、Zustand |

**为什么双轨：** 番茄钟是计时器，做成 FC 毫无意义（AI 怎么"开始一个 25 分钟"）；但"把这段对话存成笔记"做成页面手动操作又太蠢。**工具的形态应该服从用途，而非强求统一。**

**关键约束：** A 轨工具的操作对象（笔记文件、提醒数据）与 B 轨页面**共享同一存储层**。即：AI 存的笔记，用户在笔记页能看到/编辑；用户在工具页设的提醒，AI 也能查到。这保证"双轨"不是"两套孤立系统"。

### 13.2 v1.2 三个 MVP 工具详述

#### 工具 1：快速笔记（Notes）

**定位：** 本地 Markdown 笔记库，AI 可读写，用户可编辑。是 v2 知识库（RAG）的地基。

**存储（你已定：纯 Markdown 文件）：**
- 笔记根目录 = 用户在设置里指定的一个"笔记库目录"（默认 `userData/notes/`，可改）。
- 每条笔记 = 一个 `.md` 文件，frontmatter 存元数据：
  ```yaml
  ---
  id: <uuid>
  title: 标题
  tags: [工作, 周报]
  createdAt: 2026-08-03T09:00:00+08:00
  updatedAt: 2026-08-03T09:00:00+08:00
  ---
  正文 Markdown...
  ```
- **搜索**：v1.2 用简单"遍历文件 + 前端字符串匹配"（笔记 < 500 条时够用）。**v2 再加 SQLite 索引层 + 向量检索**（见 §11.2）。
- **与工作目录的关系**：笔记库目录自动加入文件工具白名单（AI 能直接读写），无需用户额外配。

**A 轨（FC 工具）：**
- `create_note(title, content, tags?)` → 新建笔记，返回路径。
- `search_notes(query)` → 全文搜笔记（返回标题+摘要列表）。
- `read_note(titleOrPath)` → 读指定笔记。
- `update_note(path, content?)` → 改笔记（走 write_file 三重防护）。

**B 轨（笔记页 UI）：**
- 左列：笔记树（按目录/标签）。
- 中：Markdown 编辑器（所见即所得，复用现有 `react-markdown` 渲染 + `textarea` 编辑切换）。
- 右列（P1）：大纲 / 反向链接。

**典型场景：**
- 对话中说"把刚才这段存成笔记" → AI 调 `create_note` → 笔记页立即可见。
- 在笔记页写会议纪要 → 拖入对话 → AI 基于它回答。

#### 工具 2：提醒 + 番茄钟（Focus & Reminders）

**定位：** 高频轻量工具。提醒区别于任务跟进（任务是"有截止日的工作"，提醒是"到点告诉我一件事"，如"10 分钟后叫我开会"）。

**提醒（A 轨 + B 轨）：**
- 数据：新表 `reminders(id, time, content, done, createdAt)`，存 SQLite。
- A 轨 FC：`set_reminder(time, content)` → AI 从对话抽提醒（类似任务抽取，但更轻，无需确认，直接入库——因为提醒无副作用、可随时删）。
- B 轨：工具页内"提醒列表"，可手动增删。
- 触发：复用现有 `node-cron` 调度器，到点弹系统通知。
- **与任务跟进的区别**：任务是工作（有完成度），提醒是信号（响一下就完）。提醒不进任务列表，避免污染。

**番茄钟（纯 B 轨）：**
- 25/5/15 三档（可配置）。
- 顶栏常驻小部件：`🍅 25:00 ▶` → 点击开始，倒计时结束弹通知。
- 数据：`pomodoro_sessions(id, startedAt, durationMin, taskId?)` 存 SQLite（可关联某任务，用于 v2 的"今天专注了多少"）。
- **不用 emoji** 🍅——用 lucide `Timer` 图标。

#### 工具 3：文档格式转换（Document Converter）

**定位：** 高频办公刚需。纯 B 轨 UI（拖文件 + 选格式 + 转换），AI 也可调。

**支持格式矩阵（v1.2 最小集）：**

| 输入 → 输出 | .md | .txt | .docx | .pdf | .html |
|---|---|---|---|---|---|
| .md | — | ✅ | ✅ | ✅ | ✅ |
| .txt | ✅ | — | ✅ | ⚠️ | ⚠️ |
| .docx | ✅ | ✅ | — | ⚠️ | ✅ |
| .pdf | ⚠️ | ⚠️ | ❌ | — | ❌ |

- ✅ = v1.2 必做；⚠️ = P1（pdf 生成需额外库如 puppeteer/pdfkit）；❌ = v2（pdf→docx 需 OCR，复杂）。
- **技术选型（v1.2）：**
  - Markdown→HTML：`marked`（已有 react-markdown 依赖链）。
  - HTML→PDF：`puppeteer-core`（不带 Chromium，调系统 Edge/Chrome）或 `pdfkit`（轻量但样式有限）—— **开发阶段实测 TV-4**。
  - Markdown/HTML→DOCX：`docx`（npm 包，纯 JS 生成）。
  - DOCX→Markdown：已有 `mammoth`（项目已装）。
- **安全**：转换在主进程，输入输出路径必须经 `resolveSafePath`（白名单或用户主动选）。

**A 轨 FC：** `convert_document(inputPath, targetFormat, outputPath?)` → AI 可在对话中"把这个 md 转成 docx"。
**B 轨 UI：** 工具页卡片，拖文件 → 选目标格式 → 输出到原目录或指定位置。

### 13.3 工具集完整蓝图（分级，其余进 v2）

> 你选了笔记/知识库、日程/提醒、效率/专注 + "办公效率如转换"。这是全貌，**只有 P0 进 v1.2，其余明确 v2/v3**。

| 工具 | 价值 | 复杂度 | 里程碑 | 说明 |
|---|---|---|---|---|
| 快速笔记 | ⭐⭐⭐⭐⭐ | 🟢 | **v1.2 P0** | 第二大脑地基 |
| 提醒 | ⭐⭐⭐⭐ | 🟢 | **v1.2 P0** | 高频、复用调度器 |
| 番茄钟 | ⭐⭐⭐ | 🟢 | **v1.2 P0** | 专注力工具 |
| 文档转换 | ⭐⭐⭐⭐ | 🟡 | **v1.2 P0** | 办公刚需 |
| 模板库（周报/纪要）| ⭐⭐⭐⭐ | 🟡 | v2 | 基于对话+任务数据 |
| 密码/账号本 | ⭐⭐⭐ | 🟠 | v2 | 加密存储，需审慎 |
| 截图标注 | ⭐⭐ | 🟠 | v2 | 系统截图 + 标注 |
| 图片 OCR | ⭐⭐⭐ | 🟠 | v2 | 本地 OCR（tesseract）|
| Markdown 表格↔Excel | ⭐⭐ | 🟡 | v2 | 转换工具延伸 |
| RAG 知识检索 | ⭐⭐⭐⭐⭐ | 🔴 | v2/v3 | 见 §11.2，需向量库 |
| 日历视图 | ⭐⭐⭐ | 🟡 | v2 | 时间维度看任务+提醒 |
| 剪贴板历史 | ⭐⭐ | 🟡 | v2 | 系统级，需权限 |

**v1.2 严守 4 个 P0**（笔记/提醒/番茄钟/转换），其余一律 v2。这是范围控制的红线。

### 13.4 数据模型补充（新增表）

**Reminders（提醒）：**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| time | datetime | 触发时间 |
| content | string | 提醒内容 |
| done | boolean | 是否已触发/取消 |
| source | enum | manual / from_chat |
| createdAt | datetime | |

**PomodoroSessions（番茄钟历史）：**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| startedAt | datetime | 开始时间 |
| durationMin | int | 时长（默认 25） |
| taskId | string? | 关联任务（可空） |
| completed | boolean | 是否完整完成（未中断） |

**Notes：** 不入库（纯 .md 文件 + frontmatter）。搜索 v1.2 走文件遍历，v2 加索引表 `notes_index(id, path, title, tags, mtime, contentPreview)`。

### 13.5 工具扩展验收（v1.2 增项）

- [ ] **V-T 笔记 AI 存**：对话里说"存成笔记"，AI 调 `create_note`，笔记页可见。
- [ ] **V-U 笔记页编辑**：能在笔记页新建/编辑/搜索/删笔记。
- [ ] **V-V 提醒 AI 设**：对话里说"10 分钟后提醒我"，AI 调 `set_reminder`，到点弹通知。
- [ ] **V-W 番茄钟**：顶栏小部件能开始/暂停/复位，结束弹通知。
- [ ] **V-X 文档转换**：拖 md 转 docx/pdf 成功；AI 能调 `convert_document`。
- [ ] **V-Y 工具页入口**：5 项导航，工具页有 3 个卡片入口。

---

## 14. v1.2 风险与对策（增项）

| 风险 | 概率 | 对策 |
|---|---|---|
| **双主题 token 迁移工作量爆炸** | 高 | 分批：先建 token + 改高频组件（消息/按钮/输入），边缘组件渐进迁移；grep 监控原生色类 |
| **lucide-react 全量导入包体积** | 中 | 用 `lucide-react/icons/{name}` 按需导入；或打包时 tree-shake（Vite 默认支持）|
| **笔记搜索性能**（文件遍历）| 中 | v1.2 限制笔记库大小（提示 < 500 条）；v2 加索引 |
| **PDF 生成库选型** | 中 | TV-4 实测 puppeteer-core vs pdfkit；puppeteer-core 依赖系统 Chrome，pdfkit 样式弱 |
| **4 个工具同时上，范围失控** | 高 | 严守 P0=4；模板库等 v2；每工具独立验证再合并 |
| **FC 工具数变多导致模型选择困难** | 中 | assembleTools 按上下文动态注册（如笔记 FC 仅在对话页注册，番茄钟不注册 FC）|
| **双轨数据不一致**（AI 存的笔记页面看不到）| 中 | 共享同一存储层（笔记库目录）；IPC 统一入口；写测试用例 |

### 技术验证项补充（v1.2）
- **TV-4**：PDF 生成库选型（puppeteer-core / pdfkit），真机测样式保真度与体积。
- **TV-5**：笔记库目录文件遍历搜索在 200 条笔记下的延迟（应 < 200ms）。

---

---

## 15. 参考两个工作台后的工具增补

> 状态：已通过架构师筛选，用户认可
> 触发：用户提供两个参考视频，要求"加功能工具参考这两个工作台"

### 15.1 参考来源与架构师筛选（必须先讲）

### 参考产品真实功能清单（已核实）

用户给了两个 B 站视频。视频简介信息密度低，我抓取了实际可验证的信息源：

| 参考产品 | 性质 | 信息源 | 功能体量 |
|---|---|---|---|
| **多宝鱼**（视频1） | 商业级 AI 原生办公平台（体制内 OA） | 在线站 `duobaoyu.work` 实抓 | **~40 个模块**：待办/日程/项目/思维导图/白板/看板/多维表格/协同文档 + AI知识库/AI报表/AI画图/AI数据处理/AI PPT/AI公文/AI Agent/AI沙盒 + OA（审批/用品/车辆/用印/会议/假期/考勤/IM）+ 体制内（内部任务/下级任务/公开任务/政区联表）+ PDF/Excel/图片工具箱 + 云盘 |
| **WorkBuddy**（视频2） | 腾讯官方 AI Agent 办公工具 | WebSearch 核实 | **20+ Skills 技能包**：MCP 可视化接入、微信直连、Skill 创建/导入/启停、任务规划、跨轮记忆、技能市场 |

### ⚠️ 架构师筛选（你授权"你筛选我认可"，我必须如实说）

**这两个都是企业级/团队级产品，不是"个人工作助手"。** 多宝鱼是体制内 OA 系统（车辆调度、用印审批、五级行政区划联表填报……），跟你的"个人本地单机"定位**完全不同**。直接照搬 = 一个人想做 40 个模块 = **必然一坨半成品**，正是前面 grilling 反复砍掉的东西。

**筛选三条标准（同时满足才纳入）：**
1. **个人单机场景高频** → 砍掉所有 OA/协作/审批/车辆/用印/考勤/政区联表
2. **与现有能力协同** → 优先选能联动"对话/任务/笔记"的，孤立工具低优先
3. **靠 AI 生成代码风险可控** → 选 AI 语料多、库成熟的，剔除非标/实验性技术

**砍掉的（即使参考产品有，也不做）：**
- ❌ OA 全套（审批/用品/车辆/用印/考勤/会议/假期/IM）—— 非个人场景
- ❌ 协同文档（Word/Excel/PPT 多人在线编辑）—— 需服务端，与"纯本地"冲突
- ❌ 多维表格 / 政区联表 —— 体制内专属，无个人价值
- ❌ AI 沙盒应用 / Python 脚本执行 / SSH / 系统计划任务 —— 安全风险，AGENTS.md 红线已禁
- ❌ 微信/QQ 远程操控 —— 超范围

**最终筛选结果（9 个增量，分三组，你已认可）：**

| 组 | 工具 | 来源参考 | 复杂度 |
|---|---|---|---|
| 笔记增强 | ① AI 笔记助手 ② 笔记内待办转任务 ③ 大纲折叠视图 | 多宝鱼（笔记+AI）+ WorkBuddy（记录即规划）| 🟢 |
| AI 产出 | ④ AI 日报/周报 ⑤ AI 生成思维导图 | 多宝鱼（AI报表+AI思维导图）| 🟡 |
| 文档/文件 | ⑥ PDF 工具箱 ⑦ 图片 OCR ⑧ 截图标注（+⑨ 表格互转并入§13文档转换）| 多宝鱼（PDF/图片工具箱）| 🟠 |

> **与 §13 的关系**：§13 定义了 4 个基础工具（笔记/提醒/番茄钟/转换）。本节是它们的**增强 + 新增**，不重复定义基础版，只写增量。

---

### 15.2 笔记增强组（3 个）

> 基础（§13.2）：纯 .md 文件 + frontmatter + 文件遍历搜索。本节在此基础上增强。

### ① AI 笔记助手
- 在笔记编辑页，AI 对当前笔记执行：**生成摘要 / 提炼待办 / 提出相关问题 / 续写**。
- 实现：新增 FC 工具 `ai_note_assist(action, content)`，复用现有 chat provider。`action ∈ {summarize, extract_todos, questions, continue}`。
- 结果以**可插入块**形式呈现，用户点"插入"才写进笔记（不静默改）。
- 数据全部本地，只把当前笔记内容发给模型 API。

### ② 笔记内待办转任务
- 解析笔记里的 `- [ ] xxx` 和 `- [x] xxx`（GitHub Flavored Markdown 任务列表语法，`remark-gfm` 已装）。
- 每个未勾选项右侧出现"转为任务"按钮 → 调用现有 `task:create_from_draft` IPC（M4 已实现），`source=from_chat` → `sourceConversationId` 留空，新增 `sourceNotePath` 字段记录来源笔记。
- 勾选状态双向同步：笔记里勾选 → 任务标记 done；任务页标记 done → 回写笔记勾选。

### ③ 大纲折叠视图（思维导图视图砍到 v2）
- 笔记编辑器升级为 **CodeMirror 6**（`@uiw/react-codemirror` + `@codemirror/lang-markdown`），支持按标题折叠（foldGutter）。
- **不做**完整思维导图视图（markmap 集成留 v2，理由：树编辑器双向同步复杂度高）。
- v1.2 只做"编辑器内大纲折叠"+ 一个简单的"右侧大纲面板"（解析 h1-h3 渲染成可点击树，点击跳转）。这是 80% 价值 / 20% 复杂度。

---

### 15.3 AI 产出组（2 个，全新模块）

### ④ AI 日报 / 周报
- **数据源全有了**：今天的任务（done 状态）+ 今天的对话 + 今天的番茄钟记录（§13 番茄钟）+ 今天的提醒。
- 触发：工具页"生成日报"按钮 / FC 工具 `generate_report(type, dateRange)`。
- AI 把上述数据汇总成 Markdown 报告，自动存入笔记库（`日报/2026-08-03.md`），用 §15.2 的笔记能力打开。
- 模板可配置（P1）：用户可自定义日报/周报的 system prompt。

### ⑤ AI 生成思维导图
- 选一段对话/一篇笔记/一个任务描述 → AI 提炼成 Markdown 大纲（`- ` 列表）→ **markmap** 渲染成思维导图。
- 技术：`markmap-lib` + `markmap-view`，自己用 `useRef/useEffect` 包 ~30 行 React 组件（社区无靠谱 wrapper，自包更可控）。
- 导出：PNG / SVG / 嵌入笔记。

---

### 15.4 文档/文件增强组（3 个）

### ⑥ PDF 工具箱
- **合并 / 拆分 / 提取页**：用 `pdf-lib`（纯客户端，稳）。
- **预览**：用 `pdfjs-dist` 渲染缩略图。
- **⚠️ 压缩明确降级**：`pdf-lib` 无真压缩能力（agent 核实），只能"重存+图片降质"。真压缩需 Ghostscript WASM 或服务端——**砍到 v2**。v1.2 PDF 工具箱只做合/拆/提取，不做压缩，UI 上压缩按钮灰掉并标注"v2"。

### ⑦ 图片 OCR
- 用 `tesseract.js` v6，本地 wasm，零后端。
- **⚠️ 中文准确度"可用"级**（agent 核实：Tesseract 中文已被 PaddleOCR 超越，但 Paddle 需 Python/WASM 依赖重）。
- **⚠️ Electron 配置是重灾区**（打包后 worker/core/lang 路径必须解包，issue #925/#1043）。
- **降级策略**：v1.2 先做"中英文混合识别"，UI 明确提示"中文复杂版面建议人工校对"；中文 `chi_sim` 语言包（~20MB）懒加载，不内置进安装包。
- 提供 TV-6 强制验证：打包后能跑，不是只 dev 能跑。

### ⑧ 截图标注
- **截图**：Electron `desktopCapturer.getSources({types:['screen']})`（主进程调，IPC 传渲染）。
- **标注**：原生 canvas 实现（画框/箭头/文字/马赛克）—— 不上 fabric.js（无官方 React wrapper，状态同步痛点）。AI 生成原生 canvas 代码出错率最低。
- **已知限制**（必须写进 UI 提示）：多显示器无法整屏截图（Chromium 限制）；macOS 需屏幕录制权限（v1.2 仅 Windows，暂不涉及）。
- 输出：复制到剪贴板 / 保存到工作目录 / 拖入对话。

### ⑨ 表格互转（并入 §13 文档转换）
- 你说得对，是文档转换的延伸。§13.2 文档转换的格式矩阵补充：
  - Markdown 表格 ↔ CSV 互转（纯字符串处理，零依赖）
  - CSV 预览（简易表格渲染）
  - Excel（.xlsx）读写用 `sheetjs`（`xlsx` 包）

---

### 15.5 技术选型表（经 agent 核实 2026 现状）

| 用途 | 选型 | 理由 | 已知坑 |
|---|---|---|---|
| Markdown 编辑器 | `@uiw/react-codemirror` + `@codemirror/lang-markdown` | 体积小、TS 一等公民、AI 生成模式固定 | 折叠需手动配 foldGutter |
| 思维导图渲染 | `markmap-lib` + `markmap-view`（自包 React） | 官方持续维护，社区 wrapper 不靠谱 | `IMarkmapOptions` 含函数不能 JSON 序列化，更新需手动 setData |
| PDF 合/拆/提取 | `pdf-lib` | 纯客户端稳，2025 多个前端 PDF 工具验证 | **无真压缩能力**；加密 PDF 支持有限 |
| PDF 预览 | `pdfjs-dist` | 渲染缩略图 | 只读，不编辑 |
| OCR | `tesseract.js` v6 | 纯 JS 零后端 | **中文一般**；Electron 打包路径是重灾区 |
| Excel/CSV | `sheetjs`（`xlsx`） | 事实标准 | 社区版功能足够 |
| 截图 | `desktopCapturer`（Electron 原生） | 官方 API | **多显示器无法整屏**；主进程调用经 IPC |
| 标注 | 原生 canvas | AI 生成出错率最低，无 wrapper 痛点 | 功能自实现，工作量在交互 |

---

### 15.6 数据模型补充

**Task 表增字段：**
| 字段 | 类型 | 说明 |
|---|---|---|
| sourceNotePath | string? | 任务来自哪篇笔记（§15.2 ② 笔记转任务） |

**新增表 ReportRecord（AI 报告记录）：**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| type | enum | daily / weekly |
| dateRange | string | 如 "2026-08-03" 或 "2026-W31" |
| notePath | string | 生成的报告笔记路径 |
| createdAt | datetime | |

**OCR / 截图 / PDF 操作**：无状态，不建表（可选加 `tool_history` 日志表，P1）。

---

### 15.7 v1.2 增补验收（在 §13.5 基础上追加）

- [x] **V-Z1 AI 笔记助手**：在笔记页能对当前笔记执行摘要/提炼待办，结果可插入。（v1.9 落地）
- [x] **V-Z2 笔记转任务**：笔记里 `- [ ]` 项能转任务。（v1.9.1 落地，单向转任务；勾选状态双向同步明确砍掉——价值低 + 易 race，用户在笔记里勾选更直接）
- [x] **V-Z3 大纲折叠**：笔记编辑器支持标题折叠 + 右侧大纲面板跳转。（v1.15 落地，textarea 保留不换 CodeMirror，右侧大纲侧栏 + heading id 锚点）
- [x] **V-Z4 AI 日报**：点"生成日报"能基于今日任务+对话+番茄钟产出报告笔记。（v1.8/v1.8.1 落地，含周报 + 自选日期 + 可取消）
- [x] **V-Z5 AI 思维导图**：选对话/笔记能生成 markmap 思维导图。（v1.12 落地，主题/素材双模式 + 笔记页预览；导出 PNG 砍——markmap SVG 转 PNG 额外复杂度，用户可截图标注替代）
- [x] **V-Z6 PDF 工具箱**：合并/拆分/提取页可用；压缩按钮灰掉标 v2。（v1.7 落地）
- [x] **V-Z7 OCR**：图片能识别出文字（中文复杂版面有提示）；**打包后能跑**（TV-6）。（v1.16 落地，本地 tesseract.js + chi_sim/eng 打包内置）
- [x] **V-Z8 截图标注**：能截屏 + 画框/箭头/文字 + 复制到剪贴板。（v1.19 落地，四件套标注 + 对象编辑 + Ctrl+滚轮缩放 + 三去向）
- [x] **V-Z9 表格互转**：md 表格 ↔ CSV 互转，CSV 预览。（v1.20 落地，csv/xlsx/md 表格互转 + SheetJS + CSV 预览）

---

### 15.8 v1.2 增补风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| **OCR 打包后不能用**（Electron worker 路径） | 高 | TV-6 强制验证打包环境；语言包懒加载；失败给"请在设置中指定语言包目录" |
| **OCR 中文准确度差，用户失望** | 高 | UI 明确提示"中文复杂版面建议人工校对"；预留云端 OCR 接口（v2） |
| **PDF 压缩做不了** | 已知 | 砍到 v2，UI 灰掉，不假装能做 |
| **markmap 集成 React 状态同步** | 中 | 不用第三方 wrapper，自包 useEffect；options 含函数不能 JSON 序列化 |
| **截图多显示器失败** | 已知 | UI 提示"单屏截图"；v2 评估屏幕拼接 |
| **CodeMirror 6 学习曲线** | 中 | AI 生成模式固定（扩展拼装）；先做最小集（编辑+折叠+高亮） |
| **9 个工具范围仍然偏大** | 高 | 严守分组推进：笔记增强 → AI 产出 → 文档增强，每组成型再开下一组 |
| **AI 日报数据不全导致报告空洞** | 中 | 报告生成前先展示"将基于以下数据"清单，让用户确认 |

### 技术验证项补充（v1.2 增补）
- **TV-6**：tesseract.js 在 electron-builder 打包后（asar 解包）能正常 createWorker，中英文识别。
- **TV-7**：markmap 在 React StrictMode 下不重复挂载、不泄漏。
- **TV-8**：desktopCapturer 在生产构建（非 dev）能截屏。

---

## 16. V2 路线图（展望）

> v1/v1.2 守住"够用且完整"。v2 每个**主题方向**都是"诱惑 + 陷阱"，别一锅端——按价值/复杂度挑 1~2 个做。
> 注：v1.2 已落地的工具不再列入此处；此处只收"明确推迟到 v2 及以后"的方向。

### 16.1 主题 A：协作与多设备（从"个人"到"小圈子真协作"）

| 项 | 为什么推迟 | v2 带来什么 | 复杂度 |
|---|---|---|---|
| **云同步** | 纯本地架构，加同步=引入服务器/冲突解决 | 多设备（公司+家里）数据一致 | 🔴 高（需后端 + 冲突合并） |
| **账号体系** | 无服务器 | 共享/付费的前置 | 🔴 高 |
| **共享任务列表** | 无协作模型 | 和朋友/同事共用一个清单 | 🔴 高（权限模型） |

**诱惑：** "小圈子可用"本来就是你提的，v2 把它做实。
**陷阱：** 这是 v1 工作量的 2~3 倍，且一旦做服务器就有运维、安全、备份责任。**建议：** 除非你真的有"和朋友共用"的场景，否则用 WebDAV/坚果云做"个人多设备同步"性价比远高于自建后端。

### 16.2 主题 B：知识沉淀（从"读一个文件"到"读你全部资料"）

| 项 | 为什么推迟 | v2 带来 | 复杂度 |
|---|---|---|---|
| **RAG 知识库** | 向量检索是独立子系统 | AI 能回答"基于我所有笔记..." | 🟠 中高 |
| **文档向量化** | 同上 | 长期记忆 | 🟠 中 |
| **全局文件搜索** | 全盘扫描复杂且危险 | "找一下我去年那份方案" | 🟠 中 |
| **笔记双链/反链** | v1.2 已砍（见 §15.2） | Obsidian 式第二大脑 | 🟠 中 |
| **笔记思维导图视图** | v1.2 已砍，只保留大纲折叠 | markmap 双向同步 | 🟠 中 |

**诱惑：** 这是从"工具"变"第二大脑"的关键一跳。
**陷阱：** RAG 质量 80% 取决于切片/embedding/重调，做不好比不做更糟（幻觉引用）。**建议：** 用成熟的本地向量库（如 Chroma/LanceDB）+ 现成 embedding，别自己造。

### 16.3 主题 C：智能升级（从"B 档工具调用"到"C 档自主 agent"）

| 项 | 为什么推迟 | v2 带来 | 复杂度 |
|---|---|---|---|
| **模型自动路由** | 路由逻辑难调 | 简单任务用便宜模型，省钱 | 🟡 中 |
| **AI 起草产出** | C 档 agent，易做烂 | "帮我写周报""自动收集资料" | 🔴 高（多步规划+执行+回溯） |
| **工作流编排** | 同上 | 固定流程自动化（晨报、周复盘） | 🟠 中高 |
| **长期记忆** | 记忆系统复杂 | AI 记得你的偏好/历史决策 | 🟠 中高 |

**诱惑：** 这是"助手"变"分身"的诱惑。
**陷阱：** C 档 agent 是行业公认难题（Manus/Devin 都饱受诟病）。**建议：** 先做"半自动"——AI 出草稿、人确认每步，而非全自主。模型自动路由性价比最高，建议优先。

### 16.4 主题 D：形态扩展

| 项 | 为什么推迟 | v2 带来 | 复杂度 |
|---|---|---|---|
| **Web 端** | 仅桌面 | 浏览器访问、跨平台免安装 | 🟠 中（需后端） |
| **移动端** | 仅桌面 | 随时记任务、收跟进通知 | 🔴 高 |
| **浏览器插件** | 仅桌面 | 网页内容一键存任务/笔记 | 🟠 中 |
| **MCP 工具生态** | 仅内置工具 | 接入第三方 MCP server 扩展能力 | 🟡 中（MCP 协议成熟） |

**诱惑：** 多端覆盖 + 生态。
**陷阱：** 多端 = 多套维护。**建议：** 浏览器插件 + MCP 生态是性价比最高的两个——插件解决"输入"（网页→任务），MCP 解决"能力"（不用自己写每个工具）。

### 16.5 主题 E：体验增强

| 项 | 价值 | 复杂度 |
|---|---|---|
| **语音输入/输出** | 口述任务、朗读回复 | 🟡 中 |
| **甘特/项目层级** | 复杂任务管理 | 🟠 中高 |
| **数据看板** | 生产力分析（番茄钟数据已有） | 🟡 中 |
| **日历视图** | 时间维度看任务+提醒 | 🟡 中 |
| **密码/账号本** | 加密存储 | 🟠 需审慎 |
| **剪贴板历史** | 系统级，需权限 | 🟡 中 |
| **PDF 压缩** | v1.2 砍掉（pdf-lib 无真压缩） | 🟠 需 Ghostscript WASM |

### 16.6 V2 推荐优先级（如果让我排）

> 综合价值/复杂度/你的场景，**如果做 v2，建议这个顺序**：

1. **模型自动路由**（主题C）—— 省钱、低风险、立竿见影
2. **数据看板**（主题E）—— v1.2 番茄钟/任务数据已具备，几乎免费的功能
3. **RAG 知识库 + 笔记双链**（主题B）—— "第二大脑"的核心价值，但要用成熟方案
4. **浏览器插件**（主题D）—— 解决输入端
5. **WebDAV 个人同步**（主题A 轻量版）—— 不自建服务器实现多设备
6. **MCP 生态**（主题D）—— 扩展能力上限

**明确排到最后/慎做：** 自建后端服务器（主题A 重）、移动端、C 档全自主 agent（主题C 重）——这三个每一个都是"另一个项目"的量级。

---

## 附录：决策溯源（统一）

> v1（grilling）+ v1.2（UI/工具扩展）全部决策汇总。

### A.1 v1 基础决策（grilling）

| 维度 | 决策 |
|---|---|
| 定位 | 自己为主、小圈子可用（纯本地，无服务器） |
| 形态 | 桌面应用 |
| 核心场景 | 多模型对话 + 任务管理（其余砍 v2） |
| 智能档位 | B 档（聊天 + 工具调用） |
| 联动方式 | AI 抽取任务 + AI 主动跟进 |
| 工具能力 | 联网搜索、读写文件、操作任务列表（不做系统命令） |
| 模型 | 国产为主（智谱 / DeepSeek） |
| 主动跟进 | 固定时间点 |
| 文件模式 | 手动拖入 + 预设工作目录 |
| 多模型用法 | 设置填 Key 手动切 + 按场景指定（P1） |
| 数据存储 | SQLite 本地库 |
| 跟进语气 | 可选人格（P1） |
| 跟进动作 | 问进展 + 改任务状态（不做起草产出） |
| 技术栈 | 你授权"我做主" → Electron + React + TS + Prisma + shadcn |

### A.2 v1.2 UI 重设计决策

| 维度 | 决策 |
|---|---|
| UI 方向 | 冷调科技风（Linear/Vercel 系），非 AI 默认蓝紫 |
| 强调色 | `#0066FF`（电光蓝），备选 `#06B6D4` |
| 主题 | 深 + 浅双主题，跟随系统 |
| 图标 | 统一 lucide-react，禁 emoji/文字/SVG 混搭 |
| 签名元素 | 无圆角消息块 + 左侧 2px role 色条 |
| 字体 | Inter（本地化）+ JetBrains Mono + 中文系统降级 |

### A.3 v1.2 工具扩展决策

| 维度 | 决策 |
|---|---|
| 笔记存储 | 纯 Markdown 文件 + frontmatter（v2 加索引） |
| 工具架构 | 双轨制（FC + 手动页面） |
| v1.2 基础工具（§13） | 快速笔记 / 提醒+番茄钟 / 文档转换（4 个 P0） |
| v1.2 增补工具（§15） | 笔记增强（AI助手/待办转任务/大纲）+ AI产出（日报/思维导图）+ 文档增强（PDF/OCR/截图/表格） |
| v1.2 总工具数 | 4 基础 + 9 增补 = 13 个，分 5 组推进 |
| 其余工具 | 全部 v2（见 §13.3 蓝图与 §16 V2 路线图） |

### A.4 v1.2 参考产品筛选决策

| 维度 | 决策 |
|---|---|
| 参考产品 | 多宝鱼（OA 平台）+ WorkBuddy（腾讯 Agent）—— 仅取个人场景工具，OA 全砍 |
| 笔记增强 | AI 助手 + 待办转任务 + 大纲折叠（思维导图视图/双链 → v2） |
| AI 产出 | 日报/周报 + 思维导图（markmap） |
| 文档增强 | PDF 工具箱（合拆提取，不压缩）+ OCR（tesseract，中文降级）+ 截图标注（原生 canvas）+ 表格互转（并入转换） |
| 编辑器 | CodeMirror 6（@uiw/react-codemirror） |

### A.5 关于使用的设计 skill（诚实声明）

用户曾指定用 `ui-ux-pro-max` skill。**该 skill 在本环境是 catalog-only 空壳**（仅元数据，无模板库/搜索脚本/CSV 数据）。v1.2 UI 设计实际基于：
- **frontend-design skill** 的设计原则（视觉身份、签名元素、反默认）
- **Open Design 默认 design-system 指引**
- **对项目实际代码的诊断**（精确到文件:行）

如果你想要 ui-ux-pro-max 的完整模式库，需先从 https://github.com/nextlevelbuilder/ui-ux-pro-max-skill 安装上游 bundle。

---

**文档结束。** 当前版本：v1.2。
推进顺序：**v1.2 UI 重设计（§12）→ §13 四基础工具 → §15 笔记增强 → AI 产出 → 文档增强**。每组独立验证再开下一组，严守范围。
