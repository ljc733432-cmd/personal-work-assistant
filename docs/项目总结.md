# 个人工作助手 · 项目总结

> 沉淀：已完成什么、改了什么、踩过哪些坑、设计思路、当前进度。给后续 AI（或人）快速理解项目用。
> 配套：`AGENTS.md`（规范/禁忌）、`CONTEXT.md`（术语）、`docs/技术决策记录.md`（ADR-001~023）。

---

## 一、项目是什么

一个**纯本地**的 Electron 桌面应用：接入国产大模型（智谱 GLM / DeepSeek）的对话助手。AI 能通过 Function Calling 自主调工具——读电脑文件、联网搜索、从对话抽任务、定时跟进待办、记笔记、设提醒、转文档、操作 PDF。所有数据存本机，不联网同步、不要服务器、不要账号。

---

## 二、技术栈（已定，勿换）

| 层 | 选型 | 备注 |
|---|---|---|
| 桌面 | Electron + electron-vite | React 19 + TS 5 |
| UI | Tailwind v4 + 手写 shadcn 风格 + **Phosphor 图标** + **recharts** | v1.3 换 Phosphor；v1.4 加 recharts |
| 状态 | Zustand | |
| 数据库 | SQLite（better-sqlite3 ≥13）+ Drizzle ORM | 规避 Prisma 打包地雷 |
| 模型调用 | OpenAI SDK `chat.completions` | 非 responses |
| 调度 | node-cron（跟进定点）+ setInterval（提醒轮询） | |
| 字体 | Space Grotesk 标题 + DM Sans 正文（@fontsource 本地化） | v1.3 |
| 文档/PDF | pdfkit（生成）+ pdf-lib（页面操作） | v1.2 生成 / v1.7 操作 |

**红线**：不 Prisma / 不 responses API / 不降级 better-sqlite3 <13 / 不渲染层调 Node / 不明文落 Key / 不云同步 / 不执行 shell。详见 AGENTS.md §6。

---

## 三、已完成功能（v1 → v1.7）

### v1（PRD §1~10，M1~M6）
- **M1 骨架**：electron-vite + React + Drizzle 落库验证
- **M2 对话完整**：多模型流式（智谱+DeepSeek）+ 多会话 + 切模型 + 上下文截断
- **M3 任务基础**：手动 CRUD（状态/优先级/截止日）
- **M4 对话→任务**：AI 抽任务草稿 → 人工确认入库（source=from_chat 溯源）
- **M5 文件功能 + 搜索半**：5 个文件工具（默认全盘+黑名单，配目录后锁定）+ 写入三重防护 + Tavily 联网搜索
- **M6 常驻+定时跟进**：托盘常驻 + node-cron + 跟进会话 + FC 改任务状态走二次确认

### v1.2（PRD §12 UI 重设计 + §13 四基础工具，M12.1~M12.10）
- **UI 重设计**：电光蓝冷调科技风 + 双主题（防闪烁）+ lucide 图标 + 签名消息块（无圆角+左侧 role 色条）+ 语义色 token 化
- **四基础工具（双轨制 ADR-013）**：提醒（set_reminder FC + 工具页）/ 番茄钟（纯 B 轨侧栏常驻）/ 笔记（纯 .md 文件 + frontmatter，A/B 双轨）/ 文档转换（pdfkit+simhei.ttf，TV-4 验证）

### v1.3（PRD 之外，UI 全面重造，M13.1~M13.8）
用户反馈「页面太单调」，用 ui-ux-pro-max skill 调研后**保留电光蓝骨架，叠加层次质感**（skill 推荐紫粉，经用户否决）：
- **Soft UI 系统**：多级 surface（surface-1/2/3）+ 柔和阴影（shadow-xs~lg）+ 电光蓝渐变点缀
- **品牌升级**：lucide→Phosphor（duotone 品牌位）/ Inter→Space Grotesk+DM Sans / 概览首页（默认首页 chat→overview）/ EmptyState 统一空状态
- **签名消息块 v2**：Soft UI 质感 + 流式 typing 三圆点动效

### v1.4（数据看板，M14.1~M14.5）
v2 路线图首个落地（PRD §16.5）。**与概览页区别**：概览=今日快照，看板=历史趋势。
- **趋势图**（recharts 3.x）：专注分钟/天（折线）+ 任务新建/天（柱状）
- **分布图**：任务状态饼图 + 专注时段柱状（深夜/早晨/下午/晚间）
- **时间范围**（7/30/全部）+ **图表模式切换**（趋势/分布，每屏只看 2 图）
- messages 走聚合 IPC（GROUP BY 按天，不传 content），其他复用现有 list

### v1.5（全应用动效升级，7 页全覆盖）
用户反馈「还是太素」，叠加动效层（不引 GSAP，纯 CSS + rAF）：
- **可复用基建**：`useCountUp` hook（数字滚动）+ `Sparkline` 组件（纯 SVG 迷你折线）+ CSS 工具类（fade-up / hero-glow 电光蓝径向光晕 / stagger-fade-up nth-child 错峰）
- **7 页统一**：header 光晕 + 标题/列表 fade-up 入场 + 卡片 hover 浮起 + 数字 count-up + StatCard sparkline
- **禁蓝紫渐变**（用户原话）：光晕用单色电光蓝 radial-gradient

### v1.6（模型档位，M15.1~M15.4）
PRD §16.3「模型自动路由」的手动分层版（用户明确选「手动+自定义多档」不做自动判定）：
- **档位 = 语义化快捷分组**：给 Provider 加用户自定义别名（如「快型」→glm-flash），对话页 select 显示档位名
- **零迁移存储**：settings KV `router.tiers` 存 JSON（不建表）
- **会话级记忆**：复用 M2 闲置字段 `conversation.defaultProviderId`
- **路由解析层预留**：`resolveProviderId(requested, ctx?)` 当前透传，ctx 为未来自动路由留挂钩

### v1.7（PDF 工具箱，M16.1~M16.3）
PRD §15.4⑥ 文档/文件增强组首个落地。纯 pdf-lib（零 native 依赖）：
- **三个操作**：合并（多 PDF→一个）/ 提取（指定页码 `1,3,5-7`）/ 拆分（按每份页数→多个）
- **页码约定**：UI 1-indexed / 内部 0-indexed，服务层 `parsePageInput` 容错
- **砍 v2**：压缩（pdf-lib 无真压缩）、预览（pdfjs-dist worker 在 Electron+Vite 配置是坑）
- 独立于 converter（converter 是格式转换 md→pdf 生成，PdfToolbox 是页面操作）

### v1.8（AI 日报/周报，M17，PRD §15.3④）
PRD §15.3 AI 产出组首个落地。数据全现成（任务/对话/番茄/提醒），AI 聚合成 Markdown 报告：
- **completedAt 精确完成时间**：tasks 表加 `completed_at` 字段（status→done 时写）。dashboard charts.tsx 此前注释「用 updatedAt 近似完成日会误导」——本次补洞。
- **老库迁移**（ADR-024）：项目无 drizzle-kit migrate，老库加列用幂等 ALTER + PRAGMA table_info 探测。
- **报告复用笔记库**（ADR-025）：写成 .md 笔记 + tag=`['日报'/'周报']`，不建 ReportRecord 表（数据复用优先）。
- **非流式生成**（ADR-010 范式）：reportGenerator.ts 照搬 taskExtractor，30s timeout + 兜底 Markdown。
- **数据聚合**：report:generate IPC 拉 tasks(按 completedAt) + messages(按 createdAt，新增 listMessagesInRange) + pomodoros(按 startedAt) + reminders(按 time)，全空拦截避免浪费 API。
- **生成前数据清单**（PRD §15.8 风险对策）：UI 先展示「将基于以下数据」让用户确认范围。

### v1.8.1（日报打磨，4 项增强）
- **修 bug：生成后历史列表不刷新**（run 成功后调 useNotesStore.refresh()，原挂载时拉一次导致新报告不出现）。
- **实时数据清单**：新增 report:preview IPC（不调模型只返计数）+ aggregateReportData 抽出（preview/generate 共用）。UI 显示「3 任务 / 12 对话 / 50 分钟 / 2 提醒」，模式/日期切换 300ms 防抖自动刷新。
- **日期范围自选**：加 custom 模式 + 两个 date input（YYYY-MM-DD→Unix 秒，本地时区）。ReportRange 类型扩成 daily/weekly/custom，标题/区间标签/模型 prompt 都适配。
- **生成中可取消**：report:generate 加 reqId 参数 + 模块级 AbortController Map + report:cancel IPC。generateReport 传 signal 给 OpenAI SDK requestOptions，被 abort 时抛 APIUserAbortError 走 err 分支。UI working 态显示「取消生成」按钮。

### v1.9（AI 笔记助手，M18，PRD §15.2①）
PRD §15.2 笔记增强组首个落地。对当前笔记执行 AI 操作，结果以「可插入块」呈现：
- **4 个操作**：summary 摘要 / todos 提炼待办 / questions 提问（可选追问）/ continue 续写。
- **复用 report.providerId**（零新配置项，与报告模型共用，语义相近都是非流式文本处理）。
- **非流式 + 可取消**（照搬 reportGenerator + report:generate 的 AbortController 模式）：noteAssistant.ts + note:ai/note:ai_cancel IPC（三处同步）。
- **内联面板**（不遮正文）：NotesPage 顶栏加「AI 助手」按钮，正文区上方展开可折叠面板。4 操作 tab + questions 输入框 + Markdown 渲染结果 + 「插入到笔记末尾/复制/关闭」。
- **不静默改笔记**（关键约束）：结果以可插入块呈现，用户点「插入」才 `setDraftContent(追加) + update` 落库。插入格式 `## AI 生成（操作名）` 分隔。

### v1.9.1（笔记转任务，M19，PRD §15.2②）
PRD §15.2 笔记增强组第 2 个。解析笔记 GFM 任务列表，未勾选项可一键转任务：
- **单向转任务**（笔记→任务）：不做任务页→笔记反向同步（价值低 + 易 race，用户在笔记里勾选更直接）。
- **数据层**：tasks 加 `source_note_path` 列（照搬 ADR-024 幂等 ALTER）+ source enum 扩 `'from_note'`。sourceNotePath 存笔记 fileName（笔记库内稳定），服务端从 noteId 解析填充，不信任前端传路径。
- **新增 IPC**：`task:create_from_note`（与 create_from_draft 平行，三处同步）。source 强制 from_note + status 恒 todo。
- **UI**：NotesPage 预览态 Markdown 上方加 NoteTodosPanel。正则解析 `- [ ]` 未勾选项，每条右侧「转任务」按钮。已转过的（title + sourceNotePath 匹配现有 from_note task）显示「✓ 已转」不可重复转。
- **不静默入库**（关键约束，与抽取草稿确认同源）：点「转任务」按钮才调 createFromNote。
- **双向同步砍掉**（PRD 原话但本项目明确不做）：任务页改状态不回写笔记，避免 race + 重复设计。

### v1.10（任务子任务 + 笔记 AI 修复，M20，两级层级）
用户需求：「去约会」大任务下挂「带礼物」子任务。两级层级（不支持子任务的子任务）。
- **数据层**：Task 加 `parent_id` 列（照搬 ADR-024 幂等 ALTER，第 3 次复用此模式）。null=根任务，非空=子任务。
- **IPC**：`task:create_subtask`（三处同步），source 跟随父任务（溯源一致），status 恒 todo。
- **UI**：TaskCard 底部加「子任务（done/total）」可折叠区。子任务以缩进 + 左竖线表示层级（勾选框+标题+hover 删除）。筛选时子任务跟随父任务（父被筛掉则子也不显示）。counts 只算根任务避免重复计数。
- **两级限制**：UI 不给子任务再加子任务的入口（数据层允许但前端约束）。
- **附带修复**：笔记 AI 摘要空内容 bug（DeepSeek 在严格禁止性指令下吞输出）。prompt 改指示性措辞（加 `## 摘要` 起始锚点，照搬 reportGenerator 风格）+ finish_reason 诊断日志。用户实测修复。

### v1.10.1（子任务完善，4 项增强）
- **A1 删根任务处理孤儿子任务**：task:delete 入参改 `{id, cascade?}`（TaskDeleteParams 类型）。cascade=true 级联删子任务。前端有子任务时 confirm「删除及其 N 个子任务」。
- **A2 子任务 inline 编辑标题**：子任务行双击或 hover 编辑 → inline input（回车/Esc/Blur）。走现有 upsert。
- **A3 子任务转根任务**：新增 `task:promote_subtask` IPC（清 parentId=null，三处同步）。子任务行 hover 显示 ArrowRight 按钮。
- **A4 父任务自动完成联动**：子任务勾选 done 后检查所有子任务是否都 done，若是且父未完成 → confirm「是否完成父任务」（不强制可拒绝）。

### v1.10.2（AI 待办对齐子任务层级）
用户需求：笔记 AI「提炼待办」应自动识别哪些是子任务。
- **prompt 改造**：noteAssistant todos prompt 引导用缩进表达层级（主待办顶格，子待办缩进 2 空格），给明确示例。
- **解析缩进建父子**：NotesPage parseTaskLines 捕获 indent（前导空格数），buildTodoTree 分组（indent=0 根，indent>0 归属上一个根）。
- **父子转任务**：根待办调 createFromNote，子待办调 createSubtask（需父已转拿到 parentId）。子任务按钮在父未转时 disabled（tooltip「先转父任务」）。已转判断区分父子。
- **复用现有 parentId**（零新字段），UI 对齐 TasksPage 子任务样式（缩进+左竖线）。

### v1.10.3~v1.10.7（任务系统打磨 + 笔记去重，5 轮快速迭代）
用户密集反馈驱动的小步打磨，每轮一个明确痛点：

- **v1.10.3 笔记待办去重**：
  - 插入去重——todos 插入时与笔记已有待办按归一化标题（normalizeTitle: 去空白+转小写+折叠空白）对比，重复不加；todos 不包「## AI 生成」标题块（直接追加任务行，不污染）。
  - 面板去重——buildTodoTree 建树时根全笔记去重、子任务父内去重。

- **v1.10.4 提炼待办不重复老内容 + 编辑器撑满**：
  - 提炼重复根因：run 把整个笔记（含已插入待办）发给 AI，老待办被反复提炼。修法：todos 操作发 AI 前剔除笔记里所有任务行（含缩进子任务），只发纯正文。
  - 编辑器太小根因：Textarea 用 `min-h-full`，在 `flex-1 overflow-y-auto` 容器里高度不生效。修法：正文容器改 `flex flex-1 flex-col overflow-hidden`；Textarea `min-h-0 flex-1` 撑满；预览态容器 `h-full overflow-y-auto` 内部滚动。

- **v1.10.5 任务手动移动层级 + AI todos 层级判断优化**：
  - 新 IPC `task:set_parent`（{id, parentId}）：移动任务到某父下或移出变根。防循环（不能移到自己下、目标已是子任务拒绝避免三级）。
  - 编辑态加「移动到」下拉，列出所有根任务（排除自己），选一个变其子任务，选「独立」变根。
  - todos prompt 加「不确定时不嵌套」原则（宁可少嵌套不要错嵌套，用户手动调整）。

- **v1.10.6 孤儿子任务 + 入口隐藏 + 编辑误清字段**（3 个 bug）：
  - 移动有子任务的根任务时子任务变孤儿不显示 → set_parent 移动前处理子任务（v1.10.7 进一步修正为「整组搬迁」）。
  - 「+ 添加子任务」按钮被 subOpen 折叠区隐藏（首次无子任务 subOpen=false，看不见入口）→ 添加块移出折叠区始终可见。
  - task:upsert 更新分支 `input.x ?? null` 会把未传字段清空（子任务 inline 改标题时 description/dueDate 被清）→ 改用 `input.x !== undefined ? input.x : existing.x`。

- **v1.10.7 移动整组搬迁**（语义修正）：
  - 移动 A（含 A1、A2）到 B 下时，A1、A2 跟着搬到 B 下与 A 平级（整组不散），而非变独立根任务丢失归属。
  - 移出变根时只改 A 自己，A 的子任务保持原样。

---

---

## 四、代码结构

```
个人工作助手/                      # 根（文档层）
├── AGENTS.md / CONTEXT.md         # 规范禁忌 / 术语表
├── docs/                          # 项目总结 + 技术决策(ADR-001~023) + 接手提示词
└── 个人工作助手-app/              # 代码
    ├── electron/                  # 主进程
    │   ├── main/index.ts          # 窗口/托盘/单实例/调度/生命周期
    │   ├── preload/index.ts       # contextBridge 白名单 IPC
    │   ├── ipc/index.ts           # 所有 IPC handler（按域分函数）
    │   ├── services/
    │   │   ├── db/                # Drizzle schema + 单例 + BOOTSTRAP_SQL
    │   │   ├── providers/         # 模型适配(factory/chat/truncate) + 路由(router) + 档位(tiers)
    │   │   ├── conversation/ search/ followup/ notes/   # 各域只读/调度/存储
    │   │   ├── taskExtractor.ts   # 任务抽取（非流式）
    │   │   ├── reportGenerator.ts # AI 日报/周报生成（非流式，v1.8）
    │   │   ├── noteAssistant.ts   # AI 笔记助手（摘要/待办/提问/续写，非流式，v1.9）
    │   │   ├── converter.ts       # 文档转换（md/docx/pdf/html/txt）
    │   │   ├── pdfToolbox.ts      # PDF 页面操作（合并/提取/拆分，v1.7）
    │   │   ├── fileTools.ts       # 文件读写核心（安全心脏：resolveSafePath）
    │   │   ├── searchTools.ts / tools.ts / secret.ts / logger.ts / sensitiveDirs.ts / systemDirs.ts
    │   └── types.ts               # 主进程共享类型
    ├── src/                       # 渲染层（React）
    │   ├── App.tsx                # 导航 7 项 + 主题 + 番茄钟
    │   ├── components/ui/         # button/card/EmptyState/icons.ts/Sparkline（v1.5）
    │   ├── pages/
    │   │   ├── overview/          # 概览首页 + nav store
    │   │   ├── dashboard/         # 数据看板（DashboardPage + charts，v1.4）
    │   │   ├── chat/ tasks/ notes/（含 AI 笔记助手面板 v1.9 + 笔记转任务 v1.9.1）  tools/（含 PdfToolbox v1.7 + ReportToolbox v1.8）  settings/（含 RouterSection v1.6 + ReportSection v1.8）
    │   ├── stores/                # Zustand（chat/tasks/providers/notes/reminders/theme/tiers/dashboard）
    │   └── lib/                   # ipc 封装 + cn + useCountUp（v1.5）
    └── electron-builder.json
```

---

## 五、踩坑精选（分层：红线级 → 高频级 → 版本专属）

### 红线级（违反会烂项目，已进 AGENTS.md §6）
- **better-sqlite3 11.x 与 Electron 42 不兼容** → 锁 ≥13.x
- **Prisma 打包地雷** → 用 Drizzle
- **回复慢 + 丢首字** → reqId 渲染层预生成 + 先订阅后 invoke
- **zustand selector 无限循环** → 不返回内联新对象/数组，用模块级常量
- **非流式 response_format 抖动** → 去掉 json_object，改提示词约束 + 容错解析
- **FC 工具需要 taskId 时 AI 一直问** → 系统提示必须把候选任务 ID+标题列出来

### 高频级（几乎每个新功能都会撞）
- **三处同步**：新增 IPC channel 必须 preload 白名单 + ipc handler + 渲染层调用，少一处就 "IPC blocked"
- **两处同步**：新增表必须 schema.ts(Drizzle) + db/index.ts 的 BOOTSTRAP_SQL
- **打包版 vs dev 混淆**：用户看打包版，改源码不重新 `npm run build:dir` 永远看不到变化
- **dev 启动即退出**：残留进程持单实例锁，`taskkill //IM electron.exe //F` + 注意打包版进程名是中文「个人工作助手.exe」
- **改主进程/preload 要重启 dev**（vite HMR 只覆盖渲染层）
- **装新依赖后 HMR 易白屏**（recharts 那次踩过）→ 装大依赖后重启 dev 清 `node_modules/.vite`

### 版本专属坑（改对应模块必读）

**v1.3 UI 坑（Tailwind v4 机制）**：
- Tailwind v4 shadow 覆盖无效（`@theme` 自引用死循环）→ `@layer utilities` 直接写 `box-shadow`
- Tailwind v4 不生成 font-display 工具类 → `@layer utilities` 自定义 `.font-display`
- 选中态用 bg-accent 像黑边 → 用 `bg-accent/10 + text-accent`
- 打包后托盘图标加载失败 → `asarUnpack: ["**/build/icon.*"]`
- 图标统一走 `@/components/ui/icons`（不直接 import @phosphor），品牌位 `weight="duotone"`
- 空状态用 `<EmptyState>`，不手写 div+小图标

**v1.4 看板坑（recharts）**：
- recharts HMR 白屏（运行中 optimize 触发 full reload）→ 装完重启 dev 清缓存
- recharts 颜色不响应 CSS 变量 → `getComputedStyle` 读值 + 监听 theme.resolved 改 key 重建图表

**v1.5 动效坑**：
- `hover:border-accent` 渲染异常（Tailwind v4 border 默认 currentColor 冲突）→ hover 只用 `hover:-translate-y-1 hover:shadow-lg`，不碰 border 变色
- sparkline 平线变噪音（数据全 0/单点）→ `< 2 个非零点` 或 `全相等` 时不渲染

**v1.6 档位坑**：
- 切 provider 闪回旧值（useEffect 依赖 providerId 导致覆盖）→ hydrate effect 只依赖 activeId + handleProviderChange 同步更新 store

**v1.8 日报坑（老库迁移）**：
- **SQLite ALTER TABLE ADD COLUMN 无 IF NOT EXISTS 语法**（与 CREATE TABLE 不同），重复执行报 `duplicate column name`。项目无 drizzle-kit migrate 框架，老库加列必须用 `PRAGMA table_info(xxx)` 探测列是否存在再 ALTER（见 ADR-024，db/index.ts getDb() 的迁移块）。**后续再加列照抄此模式**。

---

## 六、设计思路与核心理解（给后续 AI）

### 1. 第一约束：「全靠 AI 生成代码，出错率要低」
所有技术选型服从这一条：选 Electron/React/TS（语料丰富）→ 弃 Prisma（看不见运行时二进制错误）→ 锁 better-sqlite3≥13 → 强制 AGENTS.md/CONTEXT.md（持续一致理解）→ 新功能先 context7 拉文档不凭记忆写。

### 2. 双轨制（ADR-013）：FC 工具 + 手动页面
工具形态服从用途：笔记/提醒/转换做成 FC（对话里调更自然），番茄钟做成手动页面（计时器做成 FC 无意义）。**关键约束：A 轨与 B 轨共享同一存储层**（笔记库目录/reminders 表），保证不是两套孤立系统。

### 3. 安全模型：开放默认，按需收窄
文件读取默认全盘可读（黑名单守密钥/系统目录），用户配目录后锁定。写入三重防护：路径校验（resolveSafePath）+ 覆盖确认（FC 挂起）+ 回收站。**所有文件操作必经 resolveSafePath**，converter/pdfToolbox 照搬此模式。

### 4. 可挂起 FC 循环（核心技术资产）
tool handler 返回联合类型 `{kind:'result'}` 或 `{kind:'confirm', prompt, action}`，FC 循环遇 confirm 就 await confirmMap 的 Promise（前端弹窗 resolve）。这让"覆盖确认""任务状态修改二次确认"成为对话流的一等公民。

### 5. UI 哲学：冷调克制 + 质感叠加（v1.3→v1.5 演进）
v1.2 电光蓝冷调科技风是骨架。v1.3 叠加 Soft UI 层次质感（多级 surface + 柔和阴影 + Phosphor + Space Grotesk + 概览首页）。v1.5 再叠加动效层（fade-up 入场 + hover 浮起 + count-up + sparkline + 电光蓝光晕）。**克制不等于单调**——用质感而非色彩堆砌做层次，禁蓝紫渐变。

### 6. 数据策略：复用优先，零迁移为佳
- v1.4 看板：复用现有 list（数据量小前端 reduce），仅 messages 走聚合 IPC（表大不传 content）
- v1.6 档位：settings KV 存 JSON 不建表（档位少字段简单）
- v1.6 会话级记忆：复用 M2 闲置字段 `conversation.defaultProviderId`（零迁移）
- 原则：能复用不新建，能 KV 不建表，能闲置字段复用不加字段

### 7. 路由演进：手动分层是自动路由的踏脚石（v1.6）
真自动路由（分类器/规则）复杂难调（PRD 原话）。v1.6 先做手动语义分组（档位），`resolveProviderId(requested, ctx?)` 当前透传，ctx 参数为未来自动路由预留挂钩。**先把可控的做完，把不可控的留接口**。

### 8. 十个可复用核心模式（新功能照着抄）
1. **CRUD 链路**（Task 是最干净模板）：schema → factory 只读 → ipc → preload → zustand store → UI
2. **Key 加密**：apiKeyRef 引用 + safeStorage
3. **动态注入**：ToolContext getter（sources/getActiveSearchConfig/notesDir）
4. **可挂起 FC 确认**：`{kind:'confirm'}` + confirmMap
5. **per-conversation 状态**：metaByConv Record
6. **独立非流式调用**（ADR-010）：抽取/跟进走 createClientForProvider + stream:false
7. **跟进会话主进程发起**（ADR-011）：主进程内部 insert 不走 IPC
8. **双轨制**（ADR-013）：FC + 手动页面共享存储层
9. **Soft UI 三件套**（v1.3）：shadow utilities + 多级 surface + EmptyState
10. **轻量 nav store + 动效基建**（v1.3+v1.5）：跨页跳转免 router 库；useCountUp/Sparkline/fade-up 复用

### 9. 文档即代码
AGENTS.md（禁忌）、CONTEXT.md（术语）、技术决策记录（ADR-001~025）、本总结——和代码同等重要。每踩一个坑回写禁忌/ADR。

### 10. 本轮迭代（v1.8~v1.10.7）的设计理解与思路

本轮以"AI 产出 + 笔记增强 + 任务系统"为主线，5 个子方向、12 个版本迭代，核心思路沉淀如下：

- **AI 产出走"聚合现成数据 + 非流式"范式**（v1.8 日报）：数据全在本地表（任务/对话/番茄/提醒），AI 只做"汇总 + 文案润色"，不需强推理。复用 taskExtractor 的非流式调用（ADR-010），30s timeout + 兜底。报告存为 .md 笔记（tag 区分类型），不建专表——**数据复用优先**原则的极致体现（ADR-025）。

- **非流式调用的 prompt 要"指示性"不要"禁止性"**（v1.10 摘要 bug 血泪）：DeepSeek 在"不要前置说明""不要代码块包裹"这类禁止指令下偶发吞输出返回空 content。改用指示性措辞——给明确输出起始锚点（如 `## 摘要`），照搬 reportGenerator 工作正常的 prompt 风格。**经验：prompt 写"该输出什么"比"不该输出什么"更可靠**。

- **笔记增强的"不静默改"红线**（v1.9 AI 笔记助手 + v1.9.1 转任务）：与"不静默建任务"同源。AI 结果以「可插入块」呈现，用户点插入/转任务才落库。插入格式 `## AI 生成（操作名）` 分隔，保留可追溯。**控制权永远在用户手里**。

- **任务子任务的"两级限制"取舍**（v1.10~v1.10.7）：用户要"大任务挂小任务"，但无限嵌套的 UI/数据复杂度爆炸。只做两级（根→子），数据层允许（parentId 不强制）但前端约束（子任务不加子任务入口）。覆盖 95% 场景，复杂度可控。

- **AI 识别不准就给手动后路**（v1.10.5 手动移动层级）：AI 提炼待办的父子识别不 100% 准，与其反复调 prompt，不如给用户手动调整能力（编辑态「移动到」下拉 + 子任务转根按钮 + 整组搬迁）。**AI 是辅助不是权威，必须有手动修正路径**。

- **密集反馈驱动的快速打磨节奏**（v1.10.3~v1.10.7，5 轮）：用户真机测一轮反馈一个痛点 → 定位根因（多半是数据完整性或 UI 可见性）→ 最小修复 → 重新打包验证。每轮聚焦一个问题，不堆改动。**这种节奏下"攒着提交"而非每次推 GitHub**（减少噪音，用户明确要求）。

### 11. 本轮新增的可复用模式（v1.8~v1.10.7）
11. **老库加列幂等迁移**（ADR-024）：SQLite `ALTER TABLE ADD COLUMN` 无 IF NOT EXISTS，项目无 migrate 框架。用 `PRAGMA table_info(xxx)` 探测列存在再 ALTER。第 4 次复用（completedAt/sourceNotePath/parentId/...）。
12. **非流式可取消调用**（v1.8.1）：reqId + 模块级 AbortController Map + cancel IPC + OpenAI SDK requestOptions.signal。被 abort 时抛 APIUserAbortError 走 err。
13. **GFM 缩进识别层级**（v1.10.2）：`TASK_LINE_RE` 捕获 indent，buildTodoTree 分组（indent=0 根，>0 归属上一个根）。复用于笔记待办转任务 + AI todos 输出。
14. **任务层级手动调整**（v1.10.5~v1.10.7）：set_parent IPC + 整组搬迁语义（移动 A 时 A 的子任务跟着到新父下平级，不散不丢）。

---

## 七、当前进度（2026-08-04）

### 已完成（PRD v1 + v1.2~v1.10.7，真机实测 + GitHub）
- v1：PRD 全 7 里程碑（骨架/对话/任务/抽取/文件搜索/跟进）
- v1.2：UI 重设计 + §13 四基础工具（提醒/番茄钟/笔记/文档转换）
- v1.3：UI 全面重造（Soft UI + Phosphor + Space Grotesk + 概览首页）
- v1.4：数据看板（recharts 趋势/分布 + 时间范围 + 模式切换）
- v1.5：全应用动效升级（7 页光晕 + fade-up + stagger + hover + count-up + sparkline）
- v1.6：模型档位（手动语义分组 + 会话级记忆 + 路由解析层预留）
- v1.7：PDF 工具箱（pdf-lib 合并/提取/拆分）
- v1.8：AI 日报/周报（completedAt 精确完成时间 + 非流式生成 + 复用笔记库 tag）
- v1.8.1：日报打磨（历史刷新 bug + 实时数据预览 + 日期自选 + 生成可取消）
- v1.9：AI 笔记助手（摘要/待办/提问/续写 + 内联面板 + 不静默改笔记）
- v1.9.1：笔记转任务（单向 + sourceNotePath 溯源 + GFM 任务列表解析）
- v1.10：任务子任务（两级层级 + parent_id）+ 笔记 AI 摘要修复
- v1.10.1：子任务完善（级联删除/编辑/转根/联动完成）
- v1.10.2：AI 待办对齐子任务层级（缩进识别 + 父子转任务）
- v1.10.3~v1.10.7：任务系统打磨（笔记去重 / 提炼不重复老内容 / 编辑器撑满 / 手动移动层级 / 整组搬迁 + 3 个 bug 修复）

**最新打包版**：`release/0.2.2/win-unpacked/个人工作助手.exe`（v1.10.7）
**GitHub**：https://github.com/ljc733432-cmd/personal-work-assistant（main 分支）
**ADR 编号**：到 025（docs/技术决策记录.md）

### 进行中
无。v1.10.7 刚收尾。

### 下一步候选（PRD §15 + §16 剩余）
- **§15.2 笔记增强组剩余 1 个**：大纲折叠（CodeMirror 6，换编辑器独立大改）——AI 笔记助手 + 笔记转任务已落地
- **§15.3 AI 产出组剩余 1 个**：AI 思维导图（markmap）——日报/周报已落地
- **§15.4 文档/文件增强组剩余 2 个**：图片 OCR（tesseract.js，Electron 打包重灾区）/ 截图标注（desktopCapturer + 原生 canvas）
- **§16 其他**：RAG 知识库 / 浏览器插件 / WebDAV 同步 / MCP 生态
- **任务系统增强（用户提过但未做）**：②项目/分组分类（新 Project 表） / ③标签系统（新 Tag 表） / ④智能分组展示（按优先级/截止日自动分区块，纯 UI）
- **可考虑的增强**：PDF 预览缩略图（v1.7 推迟项）/ 日报日期范围自选扩展
- **§16 其他**：RAG 知识库 / 浏览器插件 / WebDAV 同步 / MCP 生态
- **可考虑的增强**：PDF 预览缩略图（pdfjs-dist worker 坑，v1.7 推迟项）/ 日报日期范围自选（v1.8 第一版固定今日/本周）

### 明确不做（PRD §2.2 砍掉 + 推迟 v2 重型项）
云同步 / 服务器 / 账号 / 多用户协作 / 执行系统命令 / PDF 压缩（v2）/ 移动端 / 自建后端

---

## 八、给后续 AI 的三句话
1. **改代码前先读 AGENTS.md 禁忌 + 相关 ADR + 本节踩坑精选**——每条都是踩坑血泪
2. **改 UI 后必须 `npm run build:dir` 重新打包**，否则用户看到的还是旧打包版（反复踩过）
3. **小步生成、逐步验证**——每个能跑通的最小块验证一次，不堆大块；新功能先 context7 拉文档
