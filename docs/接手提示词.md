# 接手提示词（复制下面整段给新 AI）

> 把下面 ``` 包起来的整段内容，复制粘贴到新 AI 对话的第一条消息里。

```
你现在接手「个人工作助手」项目。在动手前，必须先理解项目。

## 一、先读这三个文件（按顺序，必读）
1. E:\Desktop\AI\zcode\个人工作助手\AGENTS.md —— 技术栈/目录/编码规范/禁忌（每条禁忌都是踩过坑的血泪，不要违反）
2. E:\Desktop\AI\zcode\个人工作助手\CONTEXT.md —— 领域术语表（用规范词，不要自造同义词）
3. E:\Desktop\AI\zcode\个人工作助手\docs\项目总结.md —— 已完成什么、改了什么、踩过哪些坑、设计思路、当前进度（重点看「踩坑精选」和「设计思路」）

## 二、项目一句话
纯本地 Electron 桌面应用：接入国产大模型（智谱 GLM / DeepSeek）的对话助手，AI 能通过 Function Calling 自主调工具——读电脑文件、联网搜索、从对话抽任务、定时跟进待办、记笔记、设提醒、转文档、操作 PDF、识别图片文字（OCR）；还能生成 AI 日报/周报、AI 思维导图（markmap 可视化）、对笔记做摘要/提炼待办/提问/续写、任务支持无限层级子任务 + 标签 + 智能分组；笔记支持大纲导航 + 粘贴图片，对话支持发图（视觉模型直发 / 纯文本模型自动 OCR）。PRD v1 + v1.2~v1.18 全部完成。

## 三、当前状态（2026-08-07）
- 代码在 E:\Desktop\AI\zcode\个人工作助手\个人工作助手-app\
- GitHub: https://github.com/ljc733432-cmd/personal-work-assistant（主分支 main）
- **已完成并真机实测**：
  - v1：M1 骨架 / M2 对话完整 / M3 任务基础 / M4 对话→任务 / M5 文件+搜索 / M6 常驻+定时跟进
  - v1.2：UI 重设计（电光蓝冷调）+ §13 四基础工具（提醒/番茄钟/笔记/文档转换，双轨制）
  - v1.3：UI 全面重造（Soft UI 层次质感 + Phosphor 图标 + Space Grotesk 字体 + 概览首页）
  - v1.4：数据看板（recharts 趋势/分布图 + 时间范围 + 模式切换）
  - v1.5：全应用动效升级（7 页光晕 + fade-up + stagger + hover + count-up）
  - v1.6：模型档位（手动语义分组 + 会话级记忆 + 路由解析层预留）
  - v1.7：PDF 工具箱（pdf-lib 合并/提取/拆分）
  - v1.8/v1.8.1：AI 日报/周报（completedAt + 非流式 + 复用笔记库 + 实时预览 + 可取消）
  - v1.9/v1.9.1：AI 笔记助手（摘要/待办/提问/续写）+ 笔记转任务（GFM 缩进识别层级）
  - v1.10~v1.10.7：任务子任务（两级层级）+ 完善（级联删除/编辑/转根/手动移动/整组搬迁）
  - v1.10.8：任务智能分组（按截止日/优先级分区块 + 已完成折叠 + 趋势箭头替代 sparkline）+ 逻辑完成判定 + 统计口径统一 + 孤儿清理
  - v1.11：任务标签系统（tags 列 JSON + 字典 KV + 编辑/筛选/徽标）
  - v1.12：AI 思维导图（markmap 可视化 + 主题/素材双模式 + 笔记页预览）
  - v1.13：任务项目分类（**已回退**——与标签重复）
  - v1.14：任务无限层级子任务（递归 TaskNode + 环路检测 + 递归删后代 + 逻辑完成递归）
  - v1.15：笔记大纲折叠（右侧大纲侧栏 + Markdown heading id 锚点 + 预览态点击跳转）
  - v1.16：图片 OCR（本地 tesseract.js + chi_sim/eng 打包内置 + FC 工具 ocr_image，不挑模型）
  - v1.17：笔记贴图（粘贴/上传存 images/）+ 对话发图（视觉直发 / 纯文本走 OCR 自动分流）
  - v1.17.1：启动卡顿修复（textarea 解耦 providerId）+ 切板块不中断思考（ChatPage 常驻挂载）
  - v1.18：任务分组偏好持久化（settings KV tasks.groupBy）
- 最新打包版：release/0.2.5/win-unpacked/个人工作助手.exe（v1.18）
- ADR 编号到 025（docs/技术决策记录.md）
- ⚠️ **OCR 语言数据不入 git**（30MB）：新机器/新 AI 接手必须重新下载 `build/tessdata/*.traineddata.gz`，命令见项目总结 v1.16 条目，否则打包缺数据 OCR 报错

## 四、工作方式（必须遵守）
- 系统 Windows，命令行只用 Git Bash
- **小步生成、逐步验证**：每个功能拆成能独立跑通的最小块，写一块验一块
- **改代码前先读相关 ADR**（docs/技术决策记录.md，ADR-001~023），改名/改架构前先在那写明原因
- **写新功能前用 context7 拉最新 API 文档**（凭记忆写容易错，如 Tavily Bearer 鉴权、pdfkit 不支持 .ttc、recharts 3.x formatter 类型）
- **UI 设计参考 ui-ux-pro-max skill**（已安装完整 bundle 到 C:\Users\LJC\.agents\skills\ui-ux-pro-max，用 `python scripts/search.py "<query>" --domain <style|color|chart|gsap|...>` 查模式，注意 domain 名是 style/color/chart/ux/typography/icons/gsap/react/web/landing/product/google-fonts）
- 类型检查：cd 个人工作助手-app && npx tsc -p tsconfig.node.json --noEmit（主进程）；npx tsc --noEmit（渲染层）
- 启动：cd 个人工作助手-app && npm run dev
  - ⚠️ **启动即退出**：残留进程持单实例锁。先 `taskkill //IM electron.exe //F` 和 `taskkill //IM "个人工作助手.exe" //F` 清残留（注意打包版进程名是中文「个人工作助手.exe」不是 electron.exe）
  - ⚠️ **改了主进程/preload 代码要重启 dev**（vite HMR 只覆盖渲染层）
  - ⚠️ **装新依赖后 HMR 易白屏**（recharts 那次踩过）：装大依赖后重启 dev + 清 `node_modules/.vite` 缓存，别依赖 HMR
- 打包：cd 个人工作助手-app && npm run build:dir（免安装 win-unpacked）或 npm run build（NSIS 安装包）
  - ⚠️ **打包 EBUSY（app.asar 被锁）**：进程没死透或杀毒占用。等几秒重试；若持续锁死，改 package.json version 号让输出到新目录绕过（如 0.1.1→0.1.2）
  - ⚠️ **改渲染层后必须清 dist 再打包**（v1.15 血泪）：`npm run build:dir` 的 vite build 在 dist/ 有旧产物时**会跳过渲染层编译**，asar 装旧 bundle，UI 改动不生效。**必须 `rm -rf dist dist-electron` 再 build:dir**，并确认日志出现 `building client environment for production`。打包前必 `taskkill //IM "个人工作助手.exe" //F`（单实例锁常驻多个旧进程，既让新版打不开又锁目录）
- 提交：**攒一批再提交**（用户要求，不要每改一小下就推 GitHub）。commit message 用 feat(Mx):/fix(Mx):/docs: 前缀

## 五、红线（违反会烂项目，见 AGENTS.md §6 + 项目总结踩坑精选）
- ❌ 不要用 Prisma（用 Drizzle）
- ❌ 不要用 OpenAI responses API（用 chat.completions）
- ❌ 不要降级 better-sqlite3 到 <13.x
- ❌ 不要在渲染进程直接调 Node API（走 IPC）
- ❌ 不要把 API Key 明文落库（走 safeStorage，库里只存 apiKeyRef 引用）
- ❌ 所有文件操作必须经 resolveSafePath + isSensitive（直接 fs.readFile(用户路径) 是红线；converter/pdfToolbox 都照搬此模式）
- ❌ 不要做云同步/服务器/账号
- ❌ 不要执行系统命令/shell
- ❌ 不要静默建任务（抽取只产草稿，必须人工确认才入库）
- ⚠️ 新增 IPC channel 必须**三处同步**：preload 白名单 + ipc handler + registerIpcHandlers 注册，少一处就 "IPC blocked"
- ⚠️ 新增表必须**两处同步**：schema.ts(Drizzle) + db/index.ts 的 BOOTSTRAP_SQL(原始DDL)
- ⚠️ **zustand selector 不能返回内联新对象/数组**（如 `?? []`）：用模块级常量保证空态引用稳定
- ⚠️ **非流式模型调用慎用 response_format:{type:'json_object'}**：改用提示词约束 JSON + 容错解析
- ⚠️ **FC 工具需要 taskId 时，必须在系统提示里把 ID+标题列出来**（否则 AI 拿不到 ID 一直问）
- ⚠️ **DeepSeek 非流式调用 prompt 用"指示性"不用"禁止性"**：「不要前置说明」「不要代码块包裹」会让 DeepSeek 吞输出返回空 content。改用「请按以下格式输出，直接从 ## 标题开始」给明确起始锚点（照搬 reportGenerator 风格）。空内容时日志记录 finish_reason 定位（length/content_filter/stop）。
- ⚠️ **task:upsert 更新分支未传字段要用 existing 兜底**：`input.x ?? null` 会把未传字段清空（子任务 inline 改标题时 description/dueDate 被清）。用 `input.x !== undefined ? input.x : existing.x`。
- ⚠️ **可折叠区里的操作入口首次看不见**：`{open && (...)}` 折叠区内放"+ 添加"按钮时，初始 open=false 用户根本看不到入口。添加类入口要放折叠区外始终可见，列表才放折叠区内。
- ⚠️ **打包版多进程看的是旧版**：单实例锁导致新 exe 启动后焦点转到旧进程。测新功能前先 `taskkill //IM "个人工作助手.exe" //F` 杀干净所有残留（可能多个版本同时跑）。
- ⚠️ **移动任务层级要"整组搬迁"**：把有子任务的根任务 A 移到 B 下时，A 的子任务要跟着到 B 下与 A 平级（update parentId），否则变孤儿不显示。（v1.14 改：只移 A 自己，后代保持原结构，整树跟着移动）
- ⚠️ **task:delete 必须递归删所有后代**（v1.14）：无限层级下只删一级子任务，孙子变孤儿。循环收集后代 id + inArray 批量删。
- ⚠️ **set_parent 必须防环路**（v1.14）：移 A 到 B 下前递归查 A 的后代，B 在集合内拒绝（否则 A→B→...→A 成环渲染死循环）。
- ⚠️ **useMemo 必须在 if(editing) return 之前**（v1.14）：组件内提前 return 前用到的变量/Hook 若定义在 return 之后，触发 TDZ（Cannot access before initialization）白屏。
- ⚠️ **markmap 渲染必须隔离 + destroy**（v1.12）：markmap-view 的 d3 zoom 事件挂 document，残留劫持全局鼠标事件（输入框点不了）。隔离成独立子组件 + `key={markdown}` 重建 + 卸载 `destroy()`。
- ⚠️ **可视化元素必须自带语义**（v1.10.8）：裸 sparkline 折线无坐标轴无说明，用户以为是 bug。换成趋势箭头+百分比，或加「近7天」标签。
- ⚠️ **DeepSeek flash 模型结构化输出弱**（v1.12）：flash 类小模型 max_tokens 上限低，提炼待办/思维导图易 `finish_reason=length` 返回空。重试对 length 无效。容错：输入截断 + 降 max_tokens + 提示换强模型。
- ⚠️ **功能重复要敢于回退**（v1.13）：项目分类做完发现和标签重复，果断回退（保留 db 列无害残留）。做之前想清楚和现有功能的边界。
- ⚠️ **OCR 语言数据不入 git**（v1.16）：`build/tessdata/*.traineddata.gz` 约 30MB，新机器接手必须重新下载（命令见项目总结 v1.16 条目），否则打包缺数据 OCR 报错。tesseract.js + core 必须 asarUnpack。
- ⚠️ **ChatPage 必须常驻挂载**（v1.17.1）：App.tsx 用 `hidden` 控制显隐，不条件渲染。否则切板块 unmount 导致流式中断 + pendingImages/订阅丢失。**只有状态敏感页面（ChatPage）需要，其他页保持条件渲染**。
- ⚠️ **图片 base64 不入库**（v1.17）：对话发图的图片/OCR 结果当前轮用完即弃，persistTurn 只存原始文字，避免 DB 膨胀。代价是刷新会话后历史图片不显示。
- ⚠️ **共用组件加 prop 要向后兼容**（v1.15/v1.17）：Markdown 是聊天+笔记共用，加 `imgBaseUrl`/`attachments` 时不传的调用方行为不变；改 components map 时新 prop 加进 useMemo 依赖。

### UI 红线（改 UI 必读，全是踩过的坑）
- ⚠️ **Tailwind v4 shadow 覆盖无效**：`@theme` 自引用死循环。**修法：`@layer utilities` 直接写 `box-shadow`**（见 index.css 现有实现）
- ⚠️ **Tailwind v4 不生成 font-display 工具类**：只内置 sans/serif/mono。**自定义用 `@layer utilities` 定义 `.font-display`**
- ⚠️ **选中态不要用 bg-accent**（实色高饱和电光蓝，像黑边粗条）。用 `bg-accent/10 + text-accent`（淡蓝底+蓝字）
- ⚠️ **卡片 hover 不要碰 border 变色**（Tailwind v4 border 默认 currentColor，`hover:border-accent/30` 渲染异常出现多余蓝线）。**只用 `hover:-translate-y-1 hover:shadow-lg`**
- ⚠️ **打包后托盘图标加载失败**：asar 内 nativeImage 读 ico 失败。electron-builder.json 已配 `asarUnpack: ["**/build/icon.*"]`，改图标后别删这个
- ⚠️ **改 UI 后必须 `npm run build:dir` 重新打包**：用户看的是打包版，改源码不重新打包永远看不到变化。dev 验证 ≠ 打包验证
- ⚠️ **图标统一走 `@/components/ui/icons`**：lucide 名 → Phosphor 的语义化别名（不要直接从 @phosphor-icons/react import）。品牌位传 `weight="duotone"`
- ⚠️ **空状态用 EmptyState 组件**（src/components/ui/EmptyState）：Phosphor 64px duotone 大图标，不要手写 div+小图标
- ⚠️ **recharts 图表颜色不响应 CSS 变量**：`getComputedStyle` 读值 + 监听 `theme.resolved` 改 ResponsiveContainer 的 key 强制重建图表（见 dashboard/charts.tsx）
- ⚠️ **sparkline 平线是噪音**：数据 <2 个非零点或全相等时不渲染（见 components/ui/Sparkline.tsx）
- ⚠️ **禁蓝紫渐变色**（用户原话）：氛围光晕用单色电光蓝 radial-gradient，非紫粉色块

## 六、新增功能速查（按要加什么 → 改哪些文件）

### 加一个新的 FC 工具（如 xxx_tool）
1. `electron/services/tools.ts`：加 `makeXxxTool(): ToolRegistration`（def + handler），handler 返 `{kind:'result', value}` 或 `{kind:'confirm', prompt, action}`（需确认时）
2. `assembleTools(ctx)` 末尾 push 进 list（按条件注册，参考 conversationType 区分）
3. 如果工具需要外部数据（如 taskId），在 `ipc/index.ts` chat:send 注入系统提示把数据喂给模型

### 加一个新的 CRUD 实体（照搬 Task 模板，最干净）
1. `schema.ts` 加表 + `db/index.ts` BOOTSTRAP_SQL 加 DDL（**两处同步**）
2. `providers/factory.ts` 加只读查询函数（list/get）
3. `ipc/index.ts` 加 handler（upsert/delete）+ 在 registerIpcHandlers 注册
4. `preload/index.ts` ALLOWED_INVOKE 加 channel（**三处同步**）
5. `src/types/index.ts` + `electron/types.ts` 加类型
6. `src/stores/xxx.ts` 建 zustand store（照搬 tasks.ts：data/loading/error/refresh）
7. `src/pages/xxx/` 建 UI 页面

### 加一个纯客户端工具（无 FC，如 PDF 工具箱 v1.7 范式）
照搬 PdfToolbox/converter 模式：
1. `electron/services/xxxTool.ts`：服务层，路径经 resolveSafePath（照搬 converter 的 buildSources + resolveInput/resolveOutput）
2. `ipc/index.ts` 加 handler + registerIpcHandlers 注册 + preload 白名单（**三处同步**）
3. `src/pages/tools/XxxToolbox.tsx`：UI（BackHeader + 操作面板 + 状态反馈）
4. ToolsPage 加入口卡 + active 分支

### 加一个新的双轨制工具（v1.2 范式：A 轨 FC + B 轨页面）
照搬「提醒/笔记/文档转换」三选一模板：
1. 数据层（两处同步）：schema.ts 加表（或纯文件如笔记）+ BOOTSTRAP_SQL
2. A 轨 FC：tools.ts 加 makeXxxTool，assembleTools 注册（写操作走 {kind:'confirm'}）
3. B 轨页面：stores/xxx.ts + pages/xxx/，空状态用 EmptyState
4. 共享同一存储层是关键约束（PRD §13.1，ADR-013）

### 加一个新的模型调用场景（非对话主流程，如抽取/跟进）
照搬 taskExtractor.ts 范式（ADR-010）：
1. 新建 service：`createClientForProvider(providerId)` + `client.chat.completions.create({stream:false, ...})`
2. 加 timeout（30s）+ 容错解析（剥 markdown 代码块 + JSON.parse 失败返回空）
3. providerId 从 settings 读（settings:get/set KV）
4. 不要嵌进 chat:send 的 FC 循环

### 加一个新的设置项 / KV 配置（如模型档位 v1.6 范式）
1. settings 表是 KV（key/value 字符串），用 `settings:get`/`settings:set` IPC（零新 IPC）
2. key 命名：`<域>.<名>`（如 extract.providerId / router.tiers）
3. 布尔存 "true"/"false"；复杂结构存 JSON 字符串（如档位数组），store 层 parse/stringify
4. 设置页加 Section 组件（参考 ExtractSection/RouterSection）

### 加一个新的 UI 页面（v1.3+v1.5 范式）
1. `src/pages/xxx/`：主组件 + 子组件，参考 OverviewPage（数据从 store 取，EmptyState 兜底）
2. 跨页跳转用 `useNavigate` from `@/pages/overview/nav`（轻量 nav store，免 router 库）
3. App.tsx：加 Tab 类型 + 导航项 + main 区路由分支（**插在 SettingsPage 前**，否则兜底渲染设置页）
4. 图标走 `@/components/ui/icons`，卡片用 `<Card>`（已含 shadow + hover），空状态用 `<EmptyState>`
5. **动效**：header 用 `.hero-glow` + `.animate-fade-up`；列表用 `.stagger-fade-up`；数字用 `useCountUp`
6. **改完 UI 必须重新打包**（npm run build:dir），否则用户看不到

### 加图表页（v1.4 看板范式）
1. recharts：`ResponsiveContainer` 必须有明确高度的父容器（固定 `h-60` 之类）
2. 颜色读 CSS 变量 + `useThemeKey` 监听 theme.resolved 改 key 重建（见 dashboard/charts.tsx 的 useThemeKey）
3. 数据聚合：数据量小前端 reduce（复用 list），表大走聚合 IPC（GROUP BY，不传大字段）

### 加 AI 产出功能（v1.8 日报/周报范式：聚合现成数据 + 非流式）
1. 新建 service 照搬 reportGenerator.ts：`createClientForProvider` + `chat.completions.create({stream:false})` + timeout 30s + 兜底输出
2. providerId 从 settings KV 读（如 `report.providerId`），复用现有配置不新设
3. 数据聚合在 IPC handler（全量 list 后内存过滤，数据量小），全空时拦截避免浪费 API
4. 生成前加 report:preview IPC 返计数（不调模型），UI 先展示"将基于以下数据"让用户确认
5. 可取消：reqId + 模块级 AbortController Map + cancel IPC + signal 传 requestOptions
6. 结果存为 .md 笔记（复用 noteStore.createNote + tag 区分），不建专表

### 加笔记 AI 操作（v1.9 笔记助手范式）
1. service 照搬 noteAssistant.ts：4 个操作的 SYSTEM_PROMPTS（Record<Op,string>）+ assistNote 函数
2. **prompt 写指示性措辞**：每个操作给明确输出起始锚点（`## 摘要` / `## 待办`），不要用"不要 XX"禁止句
3. 复用 report.providerId（零新配置）
4. 非流式 + 可取消（照搬 report:generate 的 AbortController 模式）
5. 内联面板展示结果（不遮正文）+「插入到笔记末尾」按钮（不静默改，用户点才落库）
6. 插入格式 `## AI 生成（操作名）` 分隔，保留可追溯

### 加任务子任务/层级操作（v1.14 无限层级范式）
1. 数据层：Task 的 `parent_id`（幂等 ALTER）。null=根，非空=子。**支持任意深度嵌套**（v1.14 起）。
2. IPC：`task:create_subtask`（任意任务可作父）/ `task:set_parent`（**防环路**：递归查后代，目标在后代集合内拒绝）/ `task:promote_subtask`（转根）。
3. 删除：`task:delete` **递归删所有后代**（循环收集后代 id + inArray 批量删，否则孙子变孤儿）。
4. UI：`TaskNode` 递归组件（每个节点可加子任务/编辑/转根/删除，缩进 `paddingLeft: depth*16`）。TaskCard 子任务区委托 TaskNode。移动到下拉列全部任务（排除自身和后代）。
5. 逻辑完成：`src/lib/taskStatus.ts` 的 `isLogicallyDone` 递归判定整棵子树全 done。

### 加任务标签/分类字段（v1.11 tags 范式）
1. 数据层：tasks 加列（如 `tags` JSON 字符串），幂等 ALTER。**不加关联表**（零新表）。
2. 5 处类型同步：schema.ts / db/index.ts BOOTSTRAP_SQL + 迁移 / src/types + electron/types / factory.ts rowToTask + ipc/index.ts rowToTask / task:upsert（`!== undefined` 兜底）。
3. 字典存 settings KV（如 `tasks.tagDict`），零新 IPC。
4. UI：TaskCard 徽标区 + 编辑器（候选点选 + 输入新建）+ 筛选条下拉。
5. **坑**：`input.x ?? null` 会清空未传字段，用 `input.x !== undefined ? input.x : existing.x`。

### 加 AI 产出功能（v1.12 思维导图范式：非流式 + 可视化渲染）
照搬 reportGenerator + markmap 集成：
1. service 照搬 reportGenerator：`createClientForProvider` + `chat.completions.create({stream:false})` + timeout 30s + 兜底。
2. 复用 `report.providerId`（零新配置）。
3. 可视化渲染隔离成独立子组件（如 `MindmapView`）+ `key={content}` 强制重建 + 卸载 destroy（d3 事件清理）。
4. 结果存为 .md 笔记（tag 区分），不建专表页面也能预览（tag 判断 + 渲染组件）。
5. prompt 用**指示性**措辞（给完整输出模板 + 起始锚点），不要用「不要 XX」禁止句（DeepSeek 吞输出）。

### 加本地引擎/不挑模型能力（v1.16 OCR + v1.17 对话发图范式）
能用本地引擎解决就不依赖模型能力，符合「纯本地、不挑模型」哲学：
1. **本地引擎服务**（如 ocrService.ts）：懒加载单例 + timeout 保护 + will-quit 清理。
2. **大依赖打包内置**（如语言数据 30MB）：国内 CDN 极慢，选打包内置而非运行时下载。**不入 git**，新机器接手重新下载（文档写明命令）。electron-builder 配 asarUnpack（worker/wasm）+ extraResources（数据）。
3. **FC 工具**：照搬 read_file 范式（resolveSafePath + 首次确认 + 扩展名校验）。
4. **对话发图自动分流**：前端暂存 attachments → 主进程按模型能力分流（视觉模型启发式判断 → 多模态直发；纯文本 → 本地引擎转文字拼进 content）。用户配什么模型都有路。
5. **图片 base64 不入库**：persistTurn 只存原始文字，图片/识别结果当前轮用完即弃，避免 DB 膨胀。
6. **共用组件加 prop 向后兼容**：Markdown 加 imgBaseUrl/attachments 时不传的调用方行为不变。

### 加设置项偏好持久化（v1.18 tasks.groupBy 范式，最轻量）
记住用户上次选择（如分组维度/默认视图），照搬 tagDict 范式：
1. settings KV 存（key 命名 `<域>.<名>`），零新 IPC（复用 settings:get/set）。
2. 挂载时 invoke('settings:get', key) 读 + 切换时 invoke('settings:set', key, value) 写。
3. 读取时校验值合法性（枚举值才覆盖默认），容错坏数据。

### 改 Soft UI 样式（v1.3 视觉系统）
- 阴影：用现成 `shadow-sm/md/lg` 类（已在 index.css `@layer utilities` 定义）
- 多级背景：`bg-surface-1`（页面底）/`bg-surface-2`（卡片层）/`bg-surface-3`（浮层）
- 标题字体：`font-display`（Space Grotesk）
- 选中态：`bg-accent/10 text-accent`（不要用 bg-accent）
- 品牌图标：`weight="duotone"` + accent 色
- 动效：fade-up 入场 / hero-glow 光晕 / stagger-fade-up 错峰（index.css 已定义）

## 七、可复用核心模式（新功能照着抄，详见项目总结第六节）
1. CRUD 链路（Task 是最干净模板）
2. Key 加密（apiKeyRef 引用 + safeStorage）
3. 动态注入（ToolContext getter：sources/getActiveSearchConfig/notesDir）
4. 可挂起 FC 确认（{kind:'confirm'} + confirmMap）
5. per-conversation 状态（metaByConv Record）
6. 独立非流式调用（ADR-010）
7. 跟进会话主进程发起（ADR-011）
8. 双轨制（ADR-013）：FC + 手动页面共享存储层
9. Soft UI 三件套（v1.3）：shadow utilities + 多级 surface + EmptyState
10. 轻量 nav store + 动效基建（v1.3+v1.5）：useCountUp / Sparkline / fade-up / hero-glow
11. 数据复用优先（v1.4+v1.6）：复用 list / KV 存 JSON 不建表 / 闲置字段复用
12. 路由解析层（v1.6）：resolveProviderId 收敛转换点，为未来自动路由预留
13. 老库加列幂等迁移（ADR-024，v1.8）：PRAGMA table_info 探测 + ALTER，第 4 次复用
14. 非流式可取消调用（v1.8.1）：reqId + AbortController Map + cancel IPC + signal
15. GFM 缩进识别层级（v1.10.2）：TASK_LINE_RE 捕获 indent + buildTodoTree 分组
16. 任务层级手动调整（v1.10.5~v1.10.7）：set_parent + AI 不准给手动后路
17. 逻辑完成判定（v1.10.8→v1.14 递归）：`src/lib/taskStatus.ts`，任务完成 = 自身 done 且所有后代 done。计数/分组用逻辑完成，状态展示用原始 status。
18. 递归组件渲染树（v1.14）：TaskNode 自递归 + 动态缩进 + 防环路检测。
19. markmap 渲染隔离（v1.12）：独立子组件 + key 重建 + destroy 清 d3 事件。
20. 可视化须带语义（v1.10.8）：裸图表无标签 = bug 感，配趋势箭头/标签。
21. 本地优先不挑模型（v1.16/v1.17）：OCR 本地 tesseract / 对话发图按模型能力分流，用户配什么模型都有路。
22. 大依赖打包内置（v1.16）：语言数据 30MB 打包内置（CDN 极慢），不入 git，新机器重新下载。
23. 图片 base64 不入库（v1.17）：当前轮用完即弃，避免 DB 膨胀。
24. 流式状态敏感页常驻挂载（v1.17.1）：ChatPage 用 hidden 不条件渲染，防切板块中断流式。
25. settings KV 偏好持久化（v1.18）：挂载读 + 切换写，记住用户上次选择，零新 IPC。

## 八、下一步候选（问用户想做哪个）
- **§15.4 文档/文件增强剩余 1 个**：截图标注（desktopCapturer + 原生 canvas，原生 canvas 交互重）
- **§16 其他**：RAG 知识库 / 浏览器插件 / WebDAV 同步 / MCP 生态
- **可考虑的增强（按难度从低到高）**：
  - 笔记 AI 质量提升（flash 模型结构化输出偏弱，提示词调优 + 容错）
  - PDF 预览缩略图（pdfjs-dist worker 坑，v1.7 推迟项）
  - 任务批量操作 / 对话搜索 / 笔记标签管理页 / 快捷键体系 / 数据导出备份
- **已落地**：§15.3 AI 产出（日报/周报 + 思维导图）/ §15.2 笔记增强（AI 助手 + 转任务 + 大纲折叠 + 贴图）/ §15.4 图片（OCR + 对话发图）/ 任务系统增强（标签 + 智能分组 + 无限层级 + 分组持久化）

## 九、理解项目后，先回我
读完三个文件后，用三句话告诉我：①项目当前能做什么 ②你最需要注意的禁忌（尤其 UI 坑）③你打算做什么、怎么拆步。等我确认后再动手。
```

---

## 使用说明

1. **开新对话**时，把上面 ``` 里的内容**整段复制**作为第一条消息
2. 新 AI 会先读三个文件，然后用三句话向你汇报理解 + 拆步思路，等你确认
3. 你确认后，它按规范推进，每个小步验证一次

## 为什么要这样

- **强制先读文档**：避免新 AI 凭想象乱写，违反已有规范
- **红线集中**：把最容易踩的坑放最显眼位置（含 UI 红线：shadow 自引用 / font-display / 选中态黑边 / hover 不碰 border / 图标 asarUnpack / 改 UI 必须重新打包 / recharts 颜色 / sparkline 噪音 / 禁蓝紫渐变；含 v1.10 新坑：DeepSeek 禁止性 prompt / upsert 误清字段 / 折叠区藏入口 / 多进程看旧版 / 移动层级整组搬迁）
- **新增功能速查**：FC 工具 / CRUD 实体 / 纯客户端工具 / 双轨制 / 模型调用 / KV 配置 / UI 页面 / 图表页 / Soft UI 样式 / **AI 产出（v1.8 日报范式）** / **笔记 AI 操作（v1.9 范式）** / **任务子任务层级（v1.10 范式）** 分别改哪些文件
- **指明可复用模式**：16 个核心模式（v1 七个 + v1.3 双轨制/Soft UI/nav store + v1.4~v1.6 数据复用/路由解析层 + v1.8~v1.10 幂等迁移/可取消调用/GFM 缩进识别/任务层级调整）
- **小步验证 + 改 UI 重新打包 + 攒着提交**：符合本项目"全靠 AI 写代码"的方法论
